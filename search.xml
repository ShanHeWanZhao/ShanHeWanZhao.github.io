<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title></title>
      <link href="/2025/01/06/cms/"/>
      <url>/2025/01/06/cms/</url>
      
        <content type="html"><![CDATA[<h1 id="CMS"><a href="#CMS" class="headerlink" title="CMS"></a>CMS</h1><p>标记，<strong>清除-清除</strong>算法的老年代gc回收器，一次CMS gc会算作两次full gc，分别为初始标记和最终标记（算上的时STW次数），但在多次收集后产生的空间碎片如果影响到了对象的分配，也会才用<strong>标记-整理</strong>算法收集一次</p><p>清除算法会产生空间碎片，如果cms区预留的空闲内存不能满足新对象的分配，那么会触发<strong>Concurrent Mode Failure</strong>，这时会冻结用户线程，临时启用<strong>Serial Old</strong>收集器重新回收老年代的垃圾，全程STW，耗时很长</p><ul><li>初始标记(CMS initial mark): STW，仅标记GCRoots对象的下一个可达对象，很快</li><li>并发标记(CMS concurent mark)</li><li>重新标记(CMS remark): STW，<a href="#%E6%A0%87%E8%AE%B0%E5%AF%B9%E8%B1%A1%E6%97%B6-%E5%B9%B6%E5%8F%91%E7%9A%84%E5%8F%AF%E8%BE%BE%E6%80%A7%E5%88%86%E6%9E%90">解决并发标记时”那些消失的对象</a></li><li>并发清除(CMS concurrent sweep)</li></ul><h2 id="1-参数"><a href="#1-参数" class="headerlink" title="1.参数"></a>1.参数</h2><ul><li><p><strong>-XX:+UseConcMarkSweepGC</strong>   ： 启用CMS收集器（年轻代默认使用ParNew收集器）</p></li><li><p><strong>–XX:CMSWaitDuration&#x3D;2000</strong> ： cms后台线程的轮询间隔时间（ms单位)</p></li><li><p><strong>-XX:+UseCMSInitiatingOccupancyOnly</strong> : 使用基于设定的阈值进行CMS gc，值为CMSInitiatingOccupancyFraction</p></li><li><p><strong>-XX:CMSInitiatingOccupancyFraction&#x3D;80</strong> : 在UseCMSInitiatingOccupancyOnly参数启用后生效。当CMS区（老年代）占比达到80%后，启用CMS垃圾回收。默认为-1，代表不启用，则老年代垃圾回收阈值算法为：<strong>( (100 - MinHeapFreeRatio) + (CMSTriggerRatio * MinHeapFreeRatio) &#x2F; 100.0) &#x2F; 100.0</strong> &#x3D; 92%</p></li><li><p><strong>-XX:ConcGCThreads&#x3D;2</strong> ：并发gc线程数，默认为（ParallelGCThreads+3）&#x2F; 4。ParallelGCThreads为新生代并行GC线程数，当CPU数量小于8时，ParallelGCThreads的值就是CPU的数量，当CPU数量大于8时，ParallelGCThreads的值等于3+5*cpuCount &#x2F; 8 （可用jstack查看）</p></li></ul><h2 id="2-其他"><a href="#2-其他" class="headerlink" title="2.其他"></a>2.其他</h2><h3 id="2-1cms-gc触发条件总结"><a href="#2-1cms-gc触发条件总结" class="headerlink" title="2.1cms gc触发条件总结"></a>2.1cms gc触发条件总结</h3><ul><li><a href="https://heapdump.cn/article/190389">原文</a></li><li>foreground collector  ：空间分配不够触发</li><li>background collector<ul><li>配置了<strong>ExplicitGCInvokesConcurrent</strong> 参数，且由**System.gc()**调用</li><li>根据统计数据动态计算（未配置<strong>UseCMSInitiatingOccupancyOnly</strong>时）</li><li>根据 Old Gen 情况判断</li><li><strong>jvm的悲观策略</strong>：<strong>Young GC 已经失败或者可能会失败，JVM 就认为需要进行一次 CMS GC</strong></li><li>metaspace扩容且<strong>CMSClassUnloadingEnabled</strong> 启用（默认也是启用的）</li></ul></li></ul>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title></title>
      <link href="/2025/01/06/java-yuan-ma/"/>
      <url>/2025/01/06/java-yuan-ma/</url>
      
        <content type="html"><![CDATA[<h1 id="1-HashMap"><a href="#1-HashMap" class="headerlink" title="1.HashMap"></a>1.HashMap</h1><p>重点字段：</p><ul><li><p>DEFAULT_LOAD_FACTOR&#x3D;0.75</p><figure class="highlight txt"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">默认加载因子 </span><br><span class="line">loadFactor加载因子是控制数组存放数据的疏密程度，</span><br><span class="line">loadFactor越趋近于1，那么 数组中存放的数据(entry)也就越多，</span><br><span class="line">也就越密，也就是会让链表的长度增加，loadFactor越小，也就是趋近于0，</span><br><span class="line">数组中存放的数据(entry)也就越少，也就越稀疏。</span><br><span class="line"></span><br><span class="line">loadFactor太大可能导致链表过长，查找元素效率低。</span><br><span class="line">而太小导致数组的空闲空间太多，利用率低，存放的数据会很分散。</span><br><span class="line">loadFactor的默认值为0.75f是官方给出的一个比较好的临界值。</span><br></pre></td></tr></table></figure></li><li><p>TREEIFY_THRESHOLD&#x3D;8</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">树化阈值。当桶(bucket)上的结点数大于这个值时会转成红黑树</span><br></pre></td></tr></table></figure></li><li><p>UNTREEIFY_THRESHOLD&#x3D;6</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">链表化阈值（当桶(bucket)上的结点数小于这个值时树转链表）</span><br></pre></td></tr></table></figure></li><li><p>MIN_TREEIFY_CAPACITY&#x3D;64</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">桶中结构树化对应的table的最小长度</span><br></pre></td></tr></table></figure></li><li><p>loadFactor&#x3D;0.75</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">计算阈值使用，必须大于0</span><br></pre></td></tr></table></figure></li><li><p>threshold &#x3D; loaloadFactor * 数组长度</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">扩容阈值，当HashMap拥有的元素数量大于了threshold，就会触发数组的扩容</span><br></pre></td></tr></table></figure></li></ul><h2 id="1-1-put方法："><a href="#1-1-put方法：" class="headerlink" title="1.1 put方法："></a>1.1 put方法：</h2><ul><li>如果是第一次添加元素，先初始化数组，在计算索引位置直接放入就行</li><li>计算索引后，数组中对应的那个bucket不存在，直接放到这个bucket</li><li>计算索引后，数组中对应的那个bucket存在<ul><li>equals根节点，直接替换</li><li>根节点为树形节点，代表已经树化，调用红黑树来放节点</li><li>以此遍历这个bucket链表，如果equals，直接替换。否则遍历到链表的末尾，再根据当前链表的长度来决定是放在末尾还是红黑树化<ul><li>红黑树化要求：链表的长度（算上bucket）已经大于等于了8且数组长度大于64</li></ul></li></ul></li></ul><h2 id="1-2-resize方法："><a href="#1-2-resize方法：" class="headerlink" title="1.2 resize方法："></a>1.2 resize方法：</h2><p><strong>HashMap数组的长度始终为2的次幂，且扩容时都是长度加倍。原因就是方便扩容</strong></p><blockquote><p> 试想一下，现要扩容一个已经有链表结构的数组:</p><p> ​先创建一个长度为2倍的新数组。</p><p> ​如果原数组某一个bucket（数组中的一个位置）为空或只有一个元素，不具备链表结构，那么这个bucket很好计算。</p><p> ​如果一个bucket已经有链表的结构了，那么计算当前Node在新数组的索引方法：当前Node中key的hash值 &amp; （新数组长度 - 1），因为新数组的长度始终是2的次幂，且是旧数组的2倍。所以这个计算出来的新索引要么是原索引，要么就是原索引的2倍（只取决于key的hash高一位是不是0），计算索引的核心方法如下。所以，将链表结构的bucket重新分配到新数组中就简单很多了</p></blockquote><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// e.hash &amp; oldCap == 0 就代表e的hash值（转换为2进制）高一位为0，</span></span><br><span class="line"><span class="comment">// 与（新的容量-1）相与后，其在数组的索引位置不变</span></span><br><span class="line"><span class="keyword">if</span> ((e.hash &amp; oldCap) == <span class="number">0</span>) &#123;</span><br><span class="line">    <span class="keyword">if</span> (loTail == <span class="literal">null</span>)</span><br><span class="line">        loHead = e;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        loTail.next = e;</span><br><span class="line">    loTail = e;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">else</span> &#123; <span class="comment">// 这里则高一位为1，与新的容量 &amp; 后，</span></span><br><span class="line">     <span class="comment">// 其在新数组的索引位置会增加新容量的扩大值（即原容量的大小）</span></span><br><span class="line">    <span class="keyword">if</span> (hiTail == <span class="literal">null</span>)</span><br><span class="line">        hiHead = e;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        hiTail.next = e;</span><br><span class="line">    hiTail = e;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="1-3-线程不安全"><a href="#1-3-线程不安全" class="headerlink" title="1.3 线程不安全"></a>1.3 线程不安全</h2><p>当两个线程同时put数据时，且被put的两个数据能定位到HashMap数组的相同那个bucket位置上，就可能产生一个覆盖掉另一个的可能，造成一个数据消失。</p><p><img src="/img/hashmap_concurrent.png"></p><p>如上如，当两个线程同时到达红框位置，且放在一个bucket里，就会出现被覆盖，导致一个放入的数据消失了</p><h1 id="2-LinkedHashMap"><a href="#2-LinkedHashMap" class="headerlink" title="2. LinkedHashMap"></a>2. LinkedHashMap</h1><p>LinkedHashMap既是Map，有具有排序功能，根据accessOrder的值来排序</p><p>重点字段：</p><ul><li><p>head：连接头节点</p></li><li><p>tail：链表为节点</p></li><li><p>accessOrder（默认false）：</p><blockquote><p> false: 表示按照放入顺序排序，越晚放入的元素就越排在后面。</p><p> true：按访问时间排序。最近一个被访问的数据将会排在链表末尾</p></blockquote></li></ul><h2 id="2-1-排序如何实现"><a href="#2-1-排序如何实现" class="headerlink" title="2.1 排序如何实现"></a>2.1 排序如何实现</h2><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">static</span> <span class="keyword">class</span> <span class="title class_">Entry</span>&lt;K,V&gt; <span class="keyword">extends</span> <span class="title class_">HashMap</span>.Node&lt;K,V&gt; &#123; <span class="comment">// 具有链表结构的Entry</span></span><br><span class="line">    <span class="comment">// 前一个节点和后一个节点</span></span><br><span class="line">    Entry&lt;K,V&gt; before, after;</span><br><span class="line">    Entry(<span class="type">int</span> hash, K key, V value, Node&lt;K,V&gt; next) &#123;</span><br><span class="line">        <span class="built_in">super</span>(hash, key, value, next);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>LinkedHashMap的Entry新增了<strong>before节点和after节点，所以是个双向链表结构。</strong></p><p>并重写了<strong>java.util.HashMap#newNode</strong>和<strong>java.util.LinkedHashMap#afterNodeAccess</strong>方法</p><p><strong>新节点添加到末尾</strong>：newNode方法在构造新节点时使用，会将当前结点设为tail</p><p><strong>最近一次被访问的节点移动到末尾</strong>：如果accessOrder为true，那么从map里获取数据时，会触发afterNodeAccess，将当前节点设置到tail。</p><p>LinkedHashMap并设计了很多内部的遍历器，在用map的遍历方法时，负责按顺序取出数据</p><h1 id="3-ConcurrentHashMap"><a href="#3-ConcurrentHashMap" class="headerlink" title="3. ConcurrentHashMap"></a>3. ConcurrentHashMap</h1><p>利用CAS和加锁，实现线程安全的Map数据结构</p><p>ConcurrentHashMap的数据结构和HashMap差不多，也是数组+链表实现，当链表长度大于8转化为红黑树，红黑树长度小于6退化为链表</p><h2 id="3-1-put方法"><a href="#3-1-put方法" class="headerlink" title="3.1 put方法"></a>3.1 put方法</h2><p>死循环put元素，直到操作成功才退出</p><ul><li>数组还没初始化，开始数组的初始化</li><li>数组的bucket还未被占用，CAS占用（成功了就break，失败了就代表已经被其他节点占用了，进行下一次循环进入其他if分支）</li><li>有线程正在进行扩容操作，则先帮助扩容</li><li>bucket被占用，锁住根节点，开始构造到链表的为尾节点。添加到尾节点后，在判断当前链表长度是否超过8，否则就转换为红黑树</li></ul><h2 id="3-2-为什么key和value不允许为null，而HashMap可以呢？"><a href="#3-2-为什么key和value不允许为null，而HashMap可以呢？" class="headerlink" title="3.2 为什么key和value不允许为null，而HashMap可以呢？"></a>3.2 为什么key和value不允许为null，而HashMap可以呢？</h2><blockquote><p><strong>ConcurrentHashMap如果允许key和value为null，会产生二义性</strong>。即不能确定map里本身没有这个数据，还是说有这个数据，但这个数据存的是null值。</p><p>为什么HashMap可以允许呢？因为它不会产生二义性，使用<strong>HashMap默认就是单线程下，假设我们获取key为A的数据返回了null，之后还马上可以通过containsKey来判断到底是不存在A还是A就为null（因为是单线程，不用担心其他线程会修改数据）</strong></p><p>但ConcurrentHashMap是线程安全的，也就是默认会在多线程下修改数据。假设ConcurrentHashMap支持设置null，这时线程A获取key为null的数据返回了null，此时我们不确定A在不在ConcurrentHashMap里，需要用containsKey来判断key为null是否存在于ConcurrentHashMap里。但多线程的情况下，B线程在A线程containsKey操作前添加了key为null的数据，导致A线程containsKey返回了true，导致和第一步预期不同（第一步可能是不存在key为null的数据）</p><p>综上：<strong>ConcurrentHashMap，它是为并发而生的，它是要用在并发场景中的，当我们 map.get(key)返回 null 的时候，是没办法通过 map.containsKey来准确的检测，因为在检测过程中可能会被其他线程锁修改，而导致检测结果并不可靠。</strong></p></blockquote><h1 id="4-LockSupport"><a href="#4-LockSupport" class="headerlink" title="4. LockSupport"></a>4. LockSupport</h1><p>​java中实现<strong>当前线程的阻塞和定时阻塞</strong>，并提供<strong>唤醒指定线程</strong>的工具，在内部使用sun.misc.Unsafe来实现这一系列的操作。在AQS中普遍被使用</p><p>部分核心方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 唤醒指定的线程（如果该线程被park了）</span></span><br><span class="line"><span class="comment"> * 如果线程先被unpark（解除等待）了，那么该线程下一次调用park(进入等待)则不起作用</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">unpark</span><span class="params">(Thread thread)</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (thread != <span class="literal">null</span>)</span><br><span class="line">        UNSAFE.unpark(thread);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 阻塞当前线程，并设置一个阻塞器（这个只是用来jstack查看，并不能通过notifyAll来唤醒阻塞的线程）</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">park</span><span class="params">(Object blocker)</span> &#123;</span><br><span class="line">    <span class="type">Thread</span> <span class="variable">t</span> <span class="operator">=</span> Thread.currentThread();</span><br><span class="line">    setBlocker(t, blocker);</span><br><span class="line">    UNSAFE.park(<span class="literal">false</span>, <span class="number">0L</span>);</span><br><span class="line">    setBlocker(t, <span class="literal">null</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 定时等待，阻塞当前线程指定的纳秒数，当时间到达时就自动唤醒（定时任务会调用）</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">parkNanos</span><span class="params">(<span class="type">long</span> nanos)</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (nanos &gt; <span class="number">0</span>)</span><br><span class="line">        UNSAFE.park(<span class="literal">false</span>, nanos);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 定时等待，阻塞当前线程直到指定的时间戳（deadline）到来就自动唤醒（定时任务会调用）</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">parkUntil</span><span class="params">(<span class="type">long</span> deadline)</span> &#123;</span><br><span class="line">    UNSAFE.park(<span class="literal">true</span>, deadline);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="5-AbstractQueuedSynchronizer"><a href="#5-AbstractQueuedSynchronizer" class="headerlink" title="5.AbstractQueuedSynchronizer"></a>5.AbstractQueuedSynchronizer</h1><p>重要的内部数据结构Node:</p><p>​Node是一个通用的数据结构，既充当同步队列节点，也充当等待队列的节点</p><ul><li><strong>同步队列</strong>：<strong>当线程尝试获取锁时，未获取到锁的线程会被构造成一个Node，利用CAS放入同步尾部作为尾节点，等待被唤醒。同步队列关联的是整个锁，一对一的关系。而同步队列中的Node又根据nextWaiter字段判断当前Node是共享节点还是独占节点</strong><ul><li><strong>共享节点：共享锁的实现（Semaphore、CountDownLatch等）。nextWaiter字段为固定的Node#SHARED。释放当前节点的线程后，还具有向后传播的能力（根据state的值判断是否需要释放后继共享节点里的线程）</strong></li><li><strong>互斥节点：互斥锁的实现（ReentrantLock等），nextWaiter字段为Node#EXCLUSIVE（即null），只会释放当前节点里的线程</strong></li></ul></li><li><strong>等待队列</strong>：当已经获取到锁的线程触发<strong>java.util.concurrent.locks.Condition#await()<strong>方法阻塞自己，让出锁时。<strong>会将当前线程构造成一个Node（等待节点，状态为CONDITION），利用CAS放入等待队列尾部。等待队列关联的是Condition。所以，当ReentrantLock构造多个Condition时，就有多个等待队列，ReentrantLock和等待队列可以为一对多，而Condition和等待队列时一对一</strong>。而</strong>当其他线程获取当前锁（ReentrantLock）的线程调用java.util.concurrent.locks.Condition#signal等方法时，便会将等待队列的首节点转入到同步队列的尾节点，并重新设置Node的状态</strong><ul><li><strong>等待节点：nextWaiter字段为等待队列中下一个等待节点的指针</strong></li></ul></li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">class</span> <span class="title class_">Node</span> &#123;</span><br><span class="line">    <span class="comment">// 共享锁</span></span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">Node</span> <span class="variable">SHARED</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Node</span>();</span><br><span class="line">    <span class="comment">// 互斥锁</span></span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">Node</span> <span class="variable">EXCLUSIVE</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line">    <span class="comment">//  取消获取锁</span></span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">CANCELLED</span> <span class="operator">=</span>  <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">SIGNAL</span>    <span class="operator">=</span> -<span class="number">1</span>;</span><br><span class="line">    <span class="comment">// 等待condition唤醒（等待队列才会用到这个状态）</span></span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">CONDITION</span> <span class="operator">=</span> -<span class="number">2</span>;</span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">PROPAGATE</span> <span class="operator">=</span> -<span class="number">3</span>;</span><br><span class="line"><span class="comment">// 当前节点的状态</span></span><br><span class="line">    <span class="keyword">volatile</span> <span class="type">int</span> waitStatus;</span><br><span class="line">    <span class="comment">// 同步队列专用</span></span><br><span class="line">    <span class="keyword">volatile</span> Node prev;</span><br><span class="line">    <span class="comment">// 同步队列专用</span></span><br><span class="line">    <span class="keyword">volatile</span> Node next;</span><br><span class="line">    <span class="comment">// 等待线程</span></span><br><span class="line">    <span class="keyword">volatile</span> Thread thread;</span><br><span class="line">    <span class="comment">// 1. 当前Node为同步队列中的共享节点时：SHARED</span></span><br><span class="line">    <span class="comment">// 2. 当前Node为同步队列中的独占节点时：null</span></span><br><span class="line">    <span class="comment">// 3. 当前Node为等待队列中的节点时：下一个等待节点的指针</span></span><br><span class="line">    Node nextWaiter;</span><br><span class="line"><span class="comment">// 判断当前节点是互斥锁，还是共享锁</span></span><br><span class="line">    <span class="keyword">final</span> <span class="type">boolean</span> <span class="title function_">isShared</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> nextWaiter == SHARED;</span><br><span class="line">    &#125;</span><br><span class="line"><span class="comment">// 当前节点的前驱结点</span></span><br><span class="line">    <span class="keyword">final</span> Node <span class="title function_">predecessor</span><span class="params">()</span> <span class="keyword">throws</span> NullPointerException &#123;</span><br><span class="line">        <span class="type">Node</span> <span class="variable">p</span> <span class="operator">=</span> prev;</span><br><span class="line">        <span class="keyword">if</span> (p == <span class="literal">null</span>)</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">NullPointerException</span>();</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">            <span class="keyword">return</span> p;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    Node() &#123;    <span class="comment">// Used to establish initial head or SHARED marker</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    Node(Thread thread, Node mode) &#123;     <span class="comment">// Used by addWaiter</span></span><br><span class="line">        <span class="built_in">this</span>.nextWaiter = mode;</span><br><span class="line">        <span class="built_in">this</span>.thread = thread;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    Node(Thread thread, <span class="type">int</span> waitStatus) &#123; <span class="comment">// Used by Condition</span></span><br><span class="line">        <span class="built_in">this</span>.waitStatus = waitStatus;</span><br><span class="line">        <span class="built_in">this</span>.thread = thread;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>关键字段和方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br><span class="line">103</span><br><span class="line">104</span><br><span class="line">105</span><br><span class="line">106</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 同步队列专属的头尾节点。</span></span><br><span class="line"><span class="comment">// 因为只有在同步队列里的线程才需要被唤醒。等待队列里的线程如果要被唤醒，需要先加入到同步队列</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">transient</span> <span class="keyword">volatile</span> Node head;</span><br><span class="line"><span class="keyword">private</span> <span class="keyword">transient</span> <span class="keyword">volatile</span> Node tail;</span><br><span class="line"><span class="comment">// 可获取到锁的次数</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">volatile</span> <span class="type">int</span> state;</span><br><span class="line"><span class="comment">// 自旋的阈值（纳秒）。当超时等待时间小于这个值时，就不会再暂停线程，而是自旋。因为这个时间已经很少了，考虑到阻塞线程后上线文切换会消耗时间，就没必要再阻塞了</span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">final</span> <span class="type">long</span> <span class="variable">spinForTimeoutThreshold</span> <span class="operator">=</span> <span class="number">1000L</span>;</span><br><span class="line"><span class="comment">// 获取到独占锁的线程</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">transient</span> Thread exclusiveOwnerThread;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">留给子类实现的尝试获取共享锁的方法，共享锁获取，返回AQS里state的剩余值 </span></span><br><span class="line"><span class="comment">    1：返回值 &gt; 0，代表当前线程获取成功，且state还有剩余值，表示可以继续传播给下一个共享节点线程，让其尝试获取锁 </span></span><br><span class="line"><span class="comment">    2：返回值 = 0，代表当前线程获取成功，但state值刚好被用完，那么下一个共享节点线程就不应该被唤醒了（因为这时已经获取不到state的剩余值了）</span></span><br><span class="line"><span class="comment">    3：返回值 &lt; 0，代表当前线程都没获取成功，直接获取失败，阻塞等待被其他线程唤醒后在尝试获取</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="keyword">protected</span> <span class="type">int</span> <span class="title function_">tryAcquireShared</span><span class="params">(<span class="type">int</span> arg)</span> &#123;</span><br><span class="line">    <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">UnsupportedOperationException</span>();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 获取共享锁</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">doAcquireSharedInterruptibly</span><span class="params">(<span class="type">int</span> arg)</span></span><br><span class="line">    <span class="keyword">throws</span> InterruptedException &#123;</span><br><span class="line">    <span class="keyword">final</span> <span class="type">Node</span> <span class="variable">node</span> <span class="operator">=</span> addWaiter(Node.SHARED);</span><br><span class="line">    <span class="type">boolean</span> <span class="variable">failed</span> <span class="operator">=</span> <span class="literal">true</span>;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">            <span class="keyword">final</span> <span class="type">Node</span> <span class="variable">p</span> <span class="operator">=</span> node.predecessor();</span><br><span class="line">            <span class="keyword">if</span> (p == head) &#123; <span class="comment">// 首节点的下个节点才有资格获取锁（首节点就是获取到锁的节点）</span></span><br><span class="line">                <span class="type">int</span> <span class="variable">r</span> <span class="operator">=</span> tryAcquireShared(arg);</span><br><span class="line">                <span class="keyword">if</span> (r &gt;= <span class="number">0</span>) &#123; <span class="comment">// 至少当前线程获取成功了，但可能state值已经被用完了</span></span><br><span class="line">                    <span class="comment">// 获取成功，传播给下一个共享Node，根据state的剩余值来判断是否需要唤醒下一个共享Node里的线程</span></span><br><span class="line">                    setHeadAndPropagate(node, r);</span><br><span class="line">                    p.next = <span class="literal">null</span>; <span class="comment">// help GC</span></span><br><span class="line">                    failed = <span class="literal">false</span>;</span><br><span class="line">                    <span class="keyword">return</span>;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> (shouldParkAfterFailedAcquire(p, node) &amp;&amp;</span><br><span class="line">                parkAndCheckInterrupt()) <span class="comment">// 不能获取到锁线程就park</span></span><br><span class="line">                <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">InterruptedException</span>();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (failed)</span><br><span class="line">            cancelAcquire(node);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 释放共享锁（Semaphore会使用）</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">doReleaseShared</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">        <span class="type">Node</span> <span class="variable">h</span> <span class="operator">=</span> head;</span><br><span class="line">        <span class="keyword">if</span> (h != <span class="literal">null</span> &amp;&amp; h != tail) &#123;</span><br><span class="line">            <span class="type">int</span> <span class="variable">ws</span> <span class="operator">=</span> h.waitStatus;</span><br><span class="line">            <span class="keyword">if</span> (ws == Node.SIGNAL) &#123;</span><br><span class="line">                <span class="keyword">if</span> (!compareAndSetWaitStatus(h, Node.SIGNAL, <span class="number">0</span>))</span><br><span class="line">                    <span class="keyword">continue</span>;            <span class="comment">// loop to recheck cases</span></span><br><span class="line">                unparkSuccessor(h);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> (ws == <span class="number">0</span> &amp;&amp;</span><br><span class="line">                     !compareAndSetWaitStatus(h, <span class="number">0</span>, Node.PROPAGATE))</span><br><span class="line">                <span class="keyword">continue</span>;                <span class="comment">// loop on failed CAS</span></span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (h == head)                   <span class="comment">// loop if head changed</span></span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">    将目标节点（参数node）设为同步队列的尾部（使用CAS来解决并发问题）。</span></span><br><span class="line"><span class="comment">    所以，在这整个过程中，链表中除首节点外其余节点的prev在任何时刻都不会为空；</span></span><br><span class="line"><span class="comment">    但除尾节点外其余节点的next字段有可能为空 （刚好走完第②步，还没走到第③步）</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="keyword">private</span> Node <span class="title function_">enq</span><span class="params">(<span class="keyword">final</span> Node node)</span> &#123;</span><br><span class="line">    <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">        <span class="type">Node</span> <span class="variable">t</span> <span class="operator">=</span> tail;</span><br><span class="line">        <span class="keyword">if</span> (t == <span class="literal">null</span>) &#123; <span class="comment">// 初始化同步队列，设置一个空Node为首尾节点</span></span><br><span class="line">            <span class="keyword">if</span> (compareAndSetHead(<span class="keyword">new</span> <span class="title class_">Node</span>()))</span><br><span class="line">                tail = head;</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            node.prev = t; <span class="comment">// 先将目标节点的prev设置程原尾节点 ①</span></span><br><span class="line">            <span class="keyword">if</span> (compareAndSetTail(t, node)) &#123; <span class="comment">// CAS设置尾节点 ②</span></span><br><span class="line">                t.next = node; <span class="comment">// 设置成功了，才把原尾节点的next设为目标节点（现尾节点）③</span></span><br><span class="line">                <span class="keyword">return</span> t;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 唤醒目标节点（参数node）的最近下一个可唤醒节点中的线程</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">unparkSuccessor</span><span class="params">(Node node)</span> &#123;</span><br><span class="line">    <span class="type">int</span> <span class="variable">ws</span> <span class="operator">=</span> node.waitStatus;</span><br><span class="line">    <span class="keyword">if</span> (ws &lt; <span class="number">0</span>)</span><br><span class="line">        compareAndSetWaitStatus(node, ws, <span class="number">0</span>);</span><br><span class="line">    <span class="comment">// 首节点的下个节点唤醒失败时，就从尾节点向前遍历，直到找到距首节点最近的可唤醒的节点</span></span><br><span class="line">    <span class="comment">// 目的是避免并发时（节点入队列和唤醒），倒数第二个节点（甚至不止）的next字段为空，导致拿不到其实已经入队列里的后续节点</span></span><br><span class="line">    <span class="type">Node</span> <span class="variable">s</span> <span class="operator">=</span> node.next;</span><br><span class="line">    <span class="keyword">if</span> (s == <span class="literal">null</span> || s.waitStatus &gt; <span class="number">0</span>) &#123;</span><br><span class="line">        s = <span class="literal">null</span>;</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">Node</span> <span class="variable">t</span> <span class="operator">=</span> tail; t != <span class="literal">null</span> &amp;&amp; t != node; t = t.prev)</span><br><span class="line">            <span class="keyword">if</span> (t.waitStatus &lt;= <span class="number">0</span>)</span><br><span class="line">                s = t;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (s != <span class="literal">null</span>)</span><br><span class="line">        LockSupport.unpark(s.thread);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="6-ReentrantLock"><a href="#6-ReentrantLock" class="headerlink" title="6.ReentrantLock"></a>6.ReentrantLock</h1><p>以ReentrantLock为例，分析公平锁和非公平锁的实现。</p><p>因为ReentrantLock是互斥锁，所以只允许一个线程获取到锁，所以AQS的state初始值为1，当获取到锁的线程尝试重入时，便会增加state。</p><h2 id="6-1-非公平锁（NonfairSync）："><a href="#6-1-非公平锁（NonfairSync）：" class="headerlink" title="6.1 非公平锁（NonfairSync）："></a>6.1 非公平锁（NonfairSync）：</h2><p>lock（加锁过程）：</p><ul><li>java.util.concurrent.locks.ReentrantLock.Sync#nonfairTryAcquire：利用CAS尝试设置state，能设置成功，代表获取到锁，成功返回。设置失败，代表已经被其他线程获取了锁，返回失败</li><li>返回失败后：<ul><li>将当前线程构造为Node节点，设置到同步队列的链表中</li><li>进入到<strong>java.util.concurrent.locks.AbstractQueuedSynchronizer#acquireQueued</strong>方法：死循环获取当前Node的前一个节点（<strong>同步队列的首节点是成功获取到锁的节点</strong>），如果前驱结点为首节点，当前Node才有资格获取锁。如果还是获取不到，就调用<strong>java.util.concurrent.locks.LockSupport#park</strong>(java.lang.Object)方法阻塞当前线程，等待其他线程唤醒再去竞争锁</li></ul></li></ul><p>unlock（释放锁）：</p><ul><li>java.util.concurrent.locks.ReentrantLock.Sync#tryRelease：复原state（将其归0），exclusiveOwnerThread设为null</li><li>java.util.concurrent.locks.AbstractQueuedSynchronizer#release：在tryRelease成功后，使用<strong>java.util.concurrent.locks.LockSupport#unpark</strong>方法唤醒同步队列首节点的下一个节点里的线程，让他再去尝试获取锁</li></ul><h2 id="6-2-公平锁（FairSync）："><a href="#6-2-公平锁（FairSync）：" class="headerlink" title="6.2 公平锁（FairSync）："></a>6.2 公平锁（FairSync）：</h2><p>lock（加锁过程）：</p><p>和非公平锁很像，不同的部分就在覆盖了<strong>java.util.concurrent.locks.AbstractQueuedSynchronizer#tryAcquire</strong>这个方法和非公平锁略有不同。在新的线程获取锁失败，并将自己构造为Node节点并放入同步队列链表后，还会通过调用<strong>java.util.concurrent.locks.AbstractQueuedSynchronizer#hasQueuedPredecessors</strong>方法</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"> <span class="comment">/** 查询是否有线程等待获取的时间长于当前线程</span></span><br><span class="line"><span class="comment"> * 判断是否存在队列中第二个Node(因为首节点是个空节点)，且第二个节点中的线程是否是当前线程</span></span><br><span class="line"><span class="comment"> * 也就是说：判断同步队列中当前节点是否有前驱结点</span></span><br><span class="line"><span class="comment"> * true:代表当前线程不是等待最久的线程或压根就没有等待的线程</span></span><br><span class="line"><span class="comment"> * false:在代表当前线程已经是等待最久的线程（毕竟队列越前面，则代表进去的越久）&lt;p/&gt;</span></span><br><span class="line"><span class="comment"> * 只有公平锁才需要用到这个方法，来判断当前线程是否等待时间最长</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">final</span> <span class="type">boolean</span> <span class="title function_">hasQueuedPredecessors</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="type">Node</span> <span class="variable">t</span> <span class="operator">=</span> tail; </span><br><span class="line">    <span class="type">Node</span> <span class="variable">h</span> <span class="operator">=</span> head;</span><br><span class="line">    Node s;</span><br><span class="line">    <span class="comment">// 用 h != t 来做判断是因为调用这个方法的线程此时还没有进入等待队列</span></span><br><span class="line">    <span class="comment">// 如果 h != t，则代表队列中有线程在等待获取锁</span></span><br><span class="line">    <span class="keyword">return</span> h != t &amp;&amp;</span><br><span class="line">        ((s = h.next) == <span class="literal">null</span> || s.thread != Thread.currentThread());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>来测试当前线程是否是等待最久的线程（也就是同步队列是否还有其他等待节点，因为新加的节点总是插入到队尾，弹出是总是从队首弹出，先进先出</strong>），如果是等待最近的线程，才有资格尝试获取锁</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// ReentrantLock的公平锁第一次尝试获取锁</span></span><br><span class="line"><span class="keyword">protected</span> <span class="keyword">final</span> <span class="type">boolean</span> <span class="title function_">tryAcquire</span><span class="params">(<span class="type">int</span> acquires)</span> &#123;</span><br><span class="line">        <span class="keyword">final</span> <span class="type">Thread</span> <span class="variable">current</span> <span class="operator">=</span> Thread.currentThread();</span><br><span class="line">        <span class="type">int</span> <span class="variable">c</span> <span class="operator">=</span> getState();</span><br><span class="line">        <span class="keyword">if</span> (c == <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">if</span> (!hasQueuedPredecessors() &amp;&amp; <span class="comment">// 测试当前线程是否是等待最久的线程</span></span><br><span class="line">                compareAndSetState(<span class="number">0</span>, acquires)) &#123;</span><br><span class="line">                setExclusiveOwnerThread(current);</span><br><span class="line">                <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (current == getExclusiveOwnerThread()) &#123;</span><br><span class="line">            <span class="type">int</span> <span class="variable">nextc</span> <span class="operator">=</span> c + acquires;</span><br><span class="line">            <span class="keyword">if</span> (nextc &lt; <span class="number">0</span>)</span><br><span class="line">                <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">Error</span>(<span class="string">&quot;Maximum lock count exceeded&quot;</span>);</span><br><span class="line">            setState(nextc);</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>unlock（释放锁）：和非公平锁一样</p><h2 id="6-3-总结"><a href="#6-3-总结" class="headerlink" title="6.3 总结"></a>6.3 总结</h2><h3 id="6-3-1-为什么叫公平锁和非公平锁"><a href="#6-3-1-为什么叫公平锁和非公平锁" class="headerlink" title="6.3.1 为什么叫公平锁和非公平锁"></a>6.3.1 为什么叫公平锁和非公平锁</h3><p>​根据上面的分析，<strong>公平锁在获取锁是总是会先判断当前线程是否是等待最久的线程</strong>。<strong>所以，就算是同步队列存在大量Node，且有线程第一次在获取锁，那么，下一次获取到锁的线程也一定是同步队列的首节点的下一个节点（首节点就是当前获取到锁的节点，只有获取成功了，同步才会更新首节点）</strong></p><p>​<strong>非公平锁中：对于已经进入同步队列的线程来说，也只能首节点的下一个节点里的线程能尝试获取锁。但对于还未构造成Node加入到同步队列的线程来说，这个线程和首节点的下一个节点里的线程能竞争获取锁</strong>，所以非公平。<strong>但对于已经进入同步队列的线程来说，前驱结点是一定比后面的节点先获取到锁的</strong></p><h3 id="6-3-2-各自优势"><a href="#6-3-2-各自优势" class="headerlink" title="6.3.2 各自优势"></a>6.3.2 <strong>各自优势</strong></h3><ul><li>公平锁：防止线程饥饿，分散性很好</li><li>非公平锁：更快。一是<strong>获取锁是不用判断当前线程是否是等待最久的线程</strong>。二是<strong>上下文交换没有公平锁频繁</strong>。在存在大量锁竞争的前提下，可以肯定，公平锁上下文切换很频繁，获取锁后的线程再次获取锁时是一定会阻塞的。而非公平锁则不一样，下一次获取到锁的线程仍可能是上一次获取到锁的线程，没有上下文切换</li></ul><h1 id="7-CountDownLatch"><a href="#7-CountDownLatch" class="headerlink" title="7.CountDownLatch"></a>7.CountDownLatch</h1><p>CountDownLatch为共享锁实现，只能使用一次。用来“卡点”，阻塞的线程需要等待其他线程准备好了后（countDown直到AQS里的state为0），才继续被唤醒执行后面的代码。</p><p>在CountDownLatch中，AQS里的state值并不表示可获取到锁的次数，而是<strong>java.util.concurrent.CountDownLatch#countDown</strong> state值的次数后会释放所有调用了**java.util.concurrent.CountDownLatch#await()**的线程</p><p>内部的同步器Sync主要方法</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">获取共享锁，只有AQS的state为0才能获取到</span></span><br><span class="line"><span class="comment">通过这个接口就可以猜到，当state为0时（拉下了所有门闩），总会返回1，代表获取锁成功。</span></span><br><span class="line"><span class="comment">并依次传播下去递归调用这个方法，直到同步队列的所有Node里的线程全部唤醒，这就是CountDownLatch的原理</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="keyword">protected</span> <span class="type">int</span> <span class="title function_">tryAcquireShared</span><span class="params">(<span class="type">int</span> acquires)</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> (getState() == <span class="number">0</span>) ? <span class="number">1</span> : -<span class="number">1</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 释放共享锁，state第一次被减为0才释放成功，也就表示了CountDownLatch只能用一次</span></span><br><span class="line"><span class="keyword">protected</span> <span class="type">boolean</span> <span class="title function_">tryReleaseShared</span><span class="params">(<span class="type">int</span> releases)</span> &#123;</span><br><span class="line">    <span class="comment">// Decrement count; signal when transition to zero</span></span><br><span class="line">    <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">        <span class="type">int</span> <span class="variable">c</span> <span class="operator">=</span> getState();</span><br><span class="line">        <span class="keyword">if</span> (c == <span class="number">0</span>)</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">        <span class="type">int</span> <span class="variable">nextc</span> <span class="operator">=</span> c-<span class="number">1</span>;</span><br><span class="line">        <span class="keyword">if</span> (compareAndSetState(c, nextc))</span><br><span class="line">            <span class="keyword">return</span> nextc == <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>await方法会阻塞当前线程，直到其他线程“拉下所有门闩”。阻塞的线程会构造为共享节点加入同步队列，只有队首节点的下一个节点才有资格尝试获取锁，获取不到就LockSupport#park</p><p>countDown会将state值减小1，当state将为0时，释放同步队列里的第二个共享节点里的线程。当这个线程释放后，就能成功获取到锁了，将这个事件传播下去，一次唤醒同步队列里的所有共享节点。至此，所有被阻塞的线程都被唤醒且会成功获取到锁，最终从await方法里返回</p><h1 id="8-Semaphore"><a href="#8-Semaphore" class="headerlink" title="8.Semaphore"></a>8.Semaphore</h1><p>​<strong>信号量，共享锁实现。可以利用构造器指定令牌（permits）的数量。当线程到达时，获取（acquire）指定数量的令牌，当没有可用令牌（premits为0）时，阻塞线程，等待令牌的释放（release）再被唤醒后继续执行。基于此，即可实现共享锁（permits大于1），也可实现不可重入的互斥锁（permits为1）</strong></p><p>也分为公平锁和分公平锁，其判断方式完全和ReentrantLock一致。</p><p>​<strong>非公平锁允许准备进入同步块的线程（还未加入同步队列）和同步队列中的第二个节点竞争获取锁。而公平锁则只允许同步队列中第二个节点里的线程能尝试获取锁。</strong></p><p>​<strong>其实现方式就是将state设为我们允许并发运行的线程数量，每当一个线程获取到锁后，将state - 1，如果state为0则阻塞所有准备进入同步块的线程，并将其构造为共享节点加入同步队列。每当有线程从同步块退出时，将state + 1，并根据是否非公平来唤醒同步队列的第二个节点来尝试获取锁</strong></p><h1 id="9-Condition接口"><a href="#9-Condition接口" class="headerlink" title="9.Condition接口"></a>9.Condition接口</h1><p><strong>等待通知接口，代替Object原生的wait和notify，其具体实现为AQS里的ConditionObject</strong></p><p>主要字段：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 等待队列中的首节点</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">transient</span> Node firstWaiter;</span><br><span class="line"><span class="comment">// 等待队列中的尾节点</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">transient</span> Node lastWaiter;</span><br></pre></td></tr></table></figure><p>Condition的实现是用了<strong>等待队列（但数据结构还是AQS里的Node，将Node#waitStatus设为2就表示这是等待节点）</strong></p><p><strong>只有获取到锁的线程才能调用Condition的阻塞和唤醒方法</strong></p><h2 id="9-1-Condition-await"><a href="#9-1-Condition-await" class="headerlink" title="9.1 Condition#await"></a>9.1 Condition#await</h2><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">void</span> <span class="title function_">await</span><span class="params">()</span> <span class="keyword">throws</span> InterruptedException &#123;</span><br><span class="line">    <span class="keyword">if</span> (Thread.interrupted()) <span class="comment">// 响应中断</span></span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">InterruptedException</span>();</span><br><span class="line">    <span class="type">Node</span> <span class="variable">node</span> <span class="operator">=</span> addConditionWaiter();</span><br><span class="line">    <span class="type">int</span> <span class="variable">savedState</span> <span class="operator">=</span> fullyRelease(node); <span class="comment">// 释放锁并且唤醒同步队列中的第二个节点</span></span><br><span class="line">    <span class="type">int</span> <span class="variable">interruptMode</span> <span class="operator">=</span> <span class="number">0</span>;</span><br><span class="line">    <span class="comment">// 死循环判断当前节点是否在等待队列中</span></span><br><span class="line">    <span class="comment">// 等待队列中的节点一定要阻塞，而同步队列中的节点是可以被唤醒的</span></span><br><span class="line">    <span class="keyword">while</span> (!isOnSyncQueue(node)) &#123;</span><br><span class="line">        LockSupport.park(<span class="built_in">this</span>);</span><br><span class="line">        <span class="keyword">if</span> ((interruptMode = checkInterruptWhileWaiting(node)) != <span class="number">0</span>)</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 当signal后，需要重新获取锁，要复原现场，需要重新持有上一次所持有的所有的state值</span></span><br><span class="line">    <span class="keyword">if</span> (acquireQueued(node, savedState) &amp;&amp; interruptMode != THROW_IE)</span><br><span class="line">        interruptMode = REINTERRUPT;</span><br><span class="line">    <span class="keyword">if</span> (node.nextWaiter != <span class="literal">null</span>) <span class="comment">// clean up if cancelled</span></span><br><span class="line">        unlinkCancelledWaiters();</span><br><span class="line">    <span class="keyword">if</span> (interruptMode != <span class="number">0</span>) <span class="comment">// 当前节点有中断</span></span><br><span class="line">        reportInterruptAfterWait(interruptMode);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>流程分析：</p><ul><li>首先将当前线程构造为等待节点，并加入到等待队列的末尾</li><li>其次释放锁资源（能够await的线程一定是获取到锁的），同时唤醒同步队列的第二个节点，让其尝试获取锁</li><li>死循环判断当前节点是否为同步节点（等待节点在等待队列里，是一定要阻塞的。同步节点在同步队列里，是可以并被唤醒并尝试获取锁的），await到这里线程就阻塞了</li><li>当被唤醒后，当前节点一定被加入了同步队列，再尝试获取锁，如果能获取到，代表就可以返回了。如果获取不到，就表示当前同步块被其他线程暂用了，也还是阻塞。不过下一次被唤醒后就会通过同步队列的唤醒方式来尝试获取锁</li></ul><h2 id="9-2-Condition-signal和signalAll"><a href="#9-2-Condition-signal和signalAll" class="headerlink" title="9.2 Condition#signal和signalAll"></a>9.2 Condition#signal和signalAll</h2><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">doSignal</span><span class="params">(Node first)</span> &#123;</span><br><span class="line">    <span class="keyword">do</span> &#123;</span><br><span class="line">        <span class="comment">// 将首节点的nextWaiter转移到首节点，如果nextWaiter为空，则表示队列中只有一个节点，且首尾相同</span></span><br><span class="line">        <span class="keyword">if</span> ( (firstWaiter = first.nextWaiter) == <span class="literal">null</span>)</span><br><span class="line">            lastWaiter = <span class="literal">null</span>;</span><br><span class="line">        first.nextWaiter = <span class="literal">null</span>; <span class="comment">// gc处理</span></span><br><span class="line">    &#125; <span class="keyword">while</span> (!transferForSignal(first) &amp;&amp;</span><br><span class="line">             (first = firstWaiter) != <span class="literal">null</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 将当前的等待节点转换为同步节点，并加入到同步队列的末尾</span></span><br><span class="line"><span class="keyword">final</span> <span class="type">boolean</span> <span class="title function_">transferForSignal</span><span class="params">(Node node)</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (!compareAndSetWaitStatus(node, Node.CONDITION, <span class="number">0</span>))</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">        <span class="type">Node</span> <span class="variable">p</span> <span class="operator">=</span> enq(node);</span><br><span class="line">        <span class="type">int</span> <span class="variable">ws</span> <span class="operator">=</span> p.waitStatus;</span><br><span class="line">        <span class="keyword">if</span> (ws &gt; <span class="number">0</span> || !compareAndSetWaitStatus(p, ws, Node.SIGNAL))</span><br><span class="line">            LockSupport.unpark(node.thread); <span class="comment">// 前驱节点被取消了，或者设置为SIGNAL失败</span></span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure><p>signal主要就是将等待队列的首节点转移到同步队列的为节点，signalAll则是将等待队列中的所有节点都转移到同步节点。所以signal并不能唤醒await的线程</p><h2 id="9-3-总结"><a href="#9-3-总结" class="headerlink" title="9.3 总结"></a>9.3 总结</h2><p>Condition实现了等待通知，当一个线程进入同步块后，就可以调用await，释放自己获取的锁资源，将自己阻塞。内部实现是<strong>首先将当前线程构造成一个等待节点，加入ConditionObject的等待队列的末尾，再释放锁资源，之后唤醒同步队列的第二个节点让其尝试获取锁</strong>。而当其他进入同步块的线程调用signal后，会将等待队列的首节点转移到同步队列，并将其变成同步节点，最后再使用同步队列的唤醒机制等待被唤醒。</p><p>​所以signal并不能直接唤醒一个await的线程，最佳使用案例就是消费者发送者机制，比如阻塞队列。</p><h1 id="10-ReentrantReadWriteLock"><a href="#10-ReentrantReadWriteLock" class="headerlink" title="10. ReentrantReadWriteLock"></a>10. ReentrantReadWriteLock</h1><p>​读写锁，支持并发的读或互斥的写。读写锁分别各自实现，读锁使用共享锁，写锁使用互斥锁。ReentrantReadWriteLock内部的ReadLock和WriteLock都使用了内部同一个Sync对象来实现读写加锁的功能，在Sync内，他将AQS的state转换为二进制，高十六位表示读状态位，低十六位表示写状态位。由于读是共享的，所以state的高十六位表示了当前有多少个线程在读，在此期间写锁是禁用的。而低十六位是写锁，所以只可能有一个线程，但可能数字大于1（这是就表示写锁重入了）。当写锁被占用是，读是不允许的</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> SHARED_SHIFT= <span class="number">16</span>;                     <span class="comment">//   读状态位            写状态位</span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">SHARED_UNIT</span>  <span class="operator">=</span> (<span class="number">1</span> &lt;&lt; SHARED_SHIFT); <span class="comment">// 0000000000000001 0000000000000000</span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">MAX_COUNT</span> <span class="operator">=</span> (<span class="number">1</span> &lt;&lt; SHARED_SHIFT) - <span class="number">1</span>;<span class="comment">//0000000000000000 1111111111111111</span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">EXCLUSIVE_MASK</span> <span class="operator">=</span> (<span class="number">1</span> &lt;&lt; SHARED_SHIFT) - <span class="number">1</span>;<span class="comment">//0000000000000000 1111111111111111</span></span><br><span class="line"><span class="comment">// 获取共享锁冲入次数（读锁专用）</span></span><br><span class="line"><span class="keyword">static</span> <span class="type">int</span> <span class="title function_">sharedCount</span><span class="params">(<span class="type">int</span> c)</span>    &#123; <span class="keyword">return</span> c &gt;&gt;&gt; SHARED_SHIFT; &#125;</span><br><span class="line"><span class="comment">// 获取排他锁冲入次数（写锁专用）</span></span><br><span class="line"><span class="keyword">static</span> <span class="type">int</span> <span class="title function_">exclusiveCount</span><span class="params">(<span class="type">int</span> c)</span> &#123; <span class="keyword">return</span> c &amp; EXCLUSIVE_MASK; &#125;</span><br></pre></td></tr></table></figure><p>读写锁都支持重入，但写锁只能让当前线程重入，并且要解锁时需要unlock重入的次数。</p><h1 id="11-ThreadPoolExecutor"><a href="#11-ThreadPoolExecutor" class="headerlink" title="11. ThreadPoolExecutor"></a>11. ThreadPoolExecutor</h1><h2 id="11-1-重要字段"><a href="#11-1-重要字段" class="headerlink" title="11.1 重要字段"></a>11.1 重要字段</h2><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//状态控制器，初始值： 1110 0000 0000 0000 0000 0000 0000 0000</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">final</span> <span class="type">AtomicInteger</span> <span class="variable">ctl</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">AtomicInteger</span>(ctlOf(RUNNING, <span class="number">0</span>));</span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">COUNT_BITS</span> <span class="operator">=</span> Integer.SIZE - <span class="number">3</span>; <span class="comment">// 29位</span></span><br><span class="line"><span class="comment">// 0001 1111 1111 1111 1111 1111 1111 1111</span></span><br><span class="line"><span class="comment">// 1110 0000 0000 0000 0000 0000 0000 0000 取反后</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">CAPACITY</span>   <span class="operator">=</span> (<span class="number">1</span> &lt;&lt; COUNT_BITS) - <span class="number">1</span>;</span><br><span class="line">                                                            </span><br><span class="line"></span><br><span class="line"><span class="comment">// 运行中：111 00000000000000000000000000000</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">RUNNING</span>    <span class="operator">=</span> -<span class="number">1</span> &lt;&lt; COUNT_BITS; </span><br><span class="line"><span class="comment">// 不再接受新任务的入队列，但已经入队列还未还未的任务还可以继续执行</span></span><br><span class="line"><span class="comment">// 000 00000000000000000000000000000</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">SHUTDOWN</span>   <span class="operator">=</span>  <span class="number">0</span> &lt;&lt; COUNT_BITS; </span><br><span class="line"><span class="comment">// 不接受新任务入队列，也不处理队列中的任务，中断正在处理任务的worker</span></span><br><span class="line"><span class="comment">// 001 00000000000000000000000000000</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">STOP</span>       <span class="operator">=</span>  <span class="number">1</span> &lt;&lt; COUNT_BITS; </span><br><span class="line"><span class="comment">// 全部完成，任务终止，worker数为0</span></span><br><span class="line"><span class="comment">// 010 00000000000000000000000000000</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">TIDYING</span>    <span class="operator">=</span>  <span class="number">2</span> &lt;&lt; COUNT_BITS;</span><br><span class="line"><span class="comment">// 011 00000000000000000000000000000</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">TERMINATED</span> <span class="operator">=</span>  <span class="number">3</span> &lt;&lt; COUNT_BITS;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 计算线程池的状态</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="type">int</span> <span class="title function_">runStateOf</span><span class="params">(<span class="type">int</span> c)</span>     &#123; <span class="keyword">return</span> c &amp; ~CAPACITY; &#125; <span class="comment">// 后29位为0，前3为跟随c</span></span><br><span class="line"><span class="comment">// 计算线程池有多少工作线程</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="type">int</span> <span class="title function_">workerCountOf</span><span class="params">(<span class="type">int</span> c)</span> &#123; <span class="keyword">return</span> c &amp; CAPACITY; &#125; <span class="comment">// 前3位为0，后面29为跟随 c</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="type">int</span> <span class="title function_">ctlOf</span><span class="params">(<span class="type">int</span> rs, <span class="type">int</span> wc)</span> &#123; <span class="keyword">return</span> rs | wc; &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 任务队列</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">final</span> BlockingQueue&lt;Runnable&gt; workQueue;</span><br><span class="line"><span class="comment">// 主锁</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">final</span> <span class="type">ReentrantLock</span> <span class="variable">mainLock</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ReentrantLock</span>();</span><br><span class="line"><span class="comment">// 工作线程的Set</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">final</span> HashSet&lt;Worker&gt; workers = <span class="keyword">new</span> <span class="title class_">HashSet</span>&lt;Worker&gt;();</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">final</span> <span class="type">Condition</span> <span class="variable">termination</span> <span class="operator">=</span> mainLock.newCondition();</span><br><span class="line"><span class="comment">// 池已经创建的线程最大数（一个动态值，线程池整个周期同时存在的最多线程数）</span></span><br><span class="line"><span class="keyword">private</span> <span class="type">int</span> largestPoolSize;</span><br><span class="line"><span class="comment">// 完成的任务数</span></span><br><span class="line"><span class="keyword">private</span> <span class="type">long</span> completedTaskCount;</span><br><span class="line"><span class="comment">// 创建线程的工厂</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">volatile</span> ThreadFactory threadFactory;</span><br><span class="line"><span class="comment">// 拒绝策略</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">volatile</span> RejectedExecutionHandler handler;</span><br><span class="line"><span class="comment">// 非核心线程数的保持时间</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">volatile</span> <span class="type">long</span> keepAliveTime;</span><br><span class="line"><span class="comment">// 是否允许核心线程过期</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">volatile</span> <span class="type">boolean</span> allowCoreThreadTimeOut;</span><br><span class="line"><span class="comment">// 核心线程数</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">volatile</span> <span class="type">int</span> corePoolSize;</span><br><span class="line"><span class="comment">// 最大线程数</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">volatile</span> <span class="type">int</span> maximumPoolSize;</span><br></pre></td></tr></table></figure><blockquote><p>​ThreadPoolExecutor利用一个int类型的数来同时保存当前线程池状态和工作线程的数量，高3为用来表示当前线程的状态，低29为用来保存工作线程的数量。</p><p>​ThreadPoolExecutor内部的Worker就是工作线程的抽象，每一个Worker都是一个工作线程。同时，Worker又继承了AQS可以充当锁的角色，目的是更好的让外部知道当前worker是否正在运行，worker运行时（获取到任务后开始运行）会加锁，通过测试当前worker是否加上锁或者是否可以获得当前worker的锁，便可知道worker是否繁忙，有助于worker的清理</p></blockquote><h2 id="11-2-核心方法"><a href="#11-2-核心方法" class="headerlink" title="11.2 核心方法"></a>11.2 核心方法</h2><ul><li><p>shutdown：将当前线程池状态设为SHUTDOWN状态，再中断空闲的Worker（判断Worker是否空闲就通过它的锁方法）。<strong>所以，执行了这个方法后，正在执行的任务不会被中断，且已经存在workQueue中的Runnable也可以被执行，但是不能放入新的Runnable</strong></p></li><li><p>shutdownNow：将当前线程池状态设为STOP状态，将所有Worker设置为中断位，且倒出workQueue中的所有Runnable。<strong>所以，执行了这个方法后，正在运行的任务如果检测了中断位就会立即退出，如果没检测就还是会执行完，而已经存在workQueue中的Runnable将不会被执行，会将这些Runnable返回给调用者，让调用者处理</strong></p></li></ul><p>​ThredPoolExecutor除了核心方法外，还提供了很多辅助方法。比如setCorePoolSize方法之类的set方法，是支持动态修改参数的。而prestart相关方法都支持预启动核心线程等</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 执行Runnable的方法</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">execute</span><span class="params">(Runnable command)</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (command == <span class="literal">null</span>)</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">NullPointerException</span>();</span><br><span class="line">    <span class="comment">// 获取当前线程池状态</span></span><br><span class="line">    <span class="type">int</span> <span class="variable">c</span> <span class="operator">=</span> ctl.get();</span><br><span class="line">    <span class="comment">// 判断是否小于核心线程数，是则新建线程运行任务</span></span><br><span class="line">    <span class="keyword">if</span> (workerCountOf(c) &lt; corePoolSize) &#123;</span><br><span class="line">        <span class="keyword">if</span> (addWorker(command, <span class="literal">true</span>))</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        c = ctl.get();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 核心数满了，并且当前线程池状态为Running，加到等待队列中</span></span><br><span class="line">    <span class="keyword">if</span> (isRunning(c) &amp;&amp; workQueue.offer(command)) &#123;</span><br><span class="line">        <span class="type">int</span> <span class="variable">recheck</span> <span class="operator">=</span> ctl.get();</span><br><span class="line">        <span class="keyword">if</span> (! isRunning(recheck) &amp;&amp; remove(command))</span><br><span class="line">            reject(command);</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (workerCountOf(recheck) == <span class="number">0</span>)</span><br><span class="line">            addWorker(<span class="literal">null</span>, <span class="literal">false</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 等待队列满了，新建线程，但不能大于最大线程数</span></span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span> (!addWorker(command, <span class="literal">false</span>))</span><br><span class="line">        <span class="comment">// 创建失败，直接调用拒绝策略</span></span><br><span class="line">        reject(command);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>当addWorker且成功创建Worker后，就会启动Worker，Worker实现了Runnable，</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// Worker的Runnable方法</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">run</span><span class="params">()</span> &#123;</span><br><span class="line">    runWorker(<span class="built_in">this</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">final</span> <span class="keyword">void</span> <span class="title function_">runWorker</span><span class="params">(Worker w)</span> &#123;</span><br><span class="line">    <span class="type">Thread</span> <span class="variable">wt</span> <span class="operator">=</span> Thread.currentThread();</span><br><span class="line">    <span class="type">Runnable</span> <span class="variable">task</span> <span class="operator">=</span> w.firstTask;</span><br><span class="line">    w.firstTask = <span class="literal">null</span>;</span><br><span class="line">    w.unlock(); <span class="comment">// allow interrupts</span></span><br><span class="line">    <span class="type">boolean</span> <span class="variable">completedAbruptly</span> <span class="operator">=</span> <span class="literal">true</span>;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        <span class="comment">// 利用阻塞队列，一直循环取任务执行（阻塞队列为空时会阻塞当前想取出元素的线程）</span></span><br><span class="line">        <span class="comment">// 如果getTask为null，就代表会终结当前工作线程</span></span><br><span class="line">        <span class="keyword">while</span> (task != <span class="literal">null</span> || (task = getTask()) != <span class="literal">null</span>) &#123;</span><br><span class="line">            w.lock();</span><br><span class="line">            <span class="keyword">if</span> ((runStateAtLeast(ctl.get(), STOP) ||</span><br><span class="line">                 (Thread.interrupted() &amp;&amp;</span><br><span class="line">                  runStateAtLeast(ctl.get(), STOP))) &amp;&amp;</span><br><span class="line">                !wt.isInterrupted())</span><br><span class="line">                wt.interrupt();</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                beforeExecute(wt, task); <span class="comment">// 钩子函数before</span></span><br><span class="line">                <span class="type">Throwable</span> <span class="variable">thrown</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    task.run(); <span class="comment">// 真正的运行Runnable</span></span><br><span class="line">                &#125; <span class="keyword">catch</span> (RuntimeException x) &#123;</span><br><span class="line">                    thrown = x; <span class="keyword">throw</span> x;</span><br><span class="line">                &#125; <span class="keyword">catch</span> (Error x) &#123;</span><br><span class="line">                    thrown = x; <span class="keyword">throw</span> x;</span><br><span class="line">                &#125; <span class="keyword">catch</span> (Throwable x) &#123;</span><br><span class="line">                    thrown = x; <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">Error</span>(x);</span><br><span class="line">                &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">                    afterExecute(task, thrown); <span class="comment">// 钩子函数after</span></span><br><span class="line">                &#125;</span><br><span class="line">            &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">                task = <span class="literal">null</span>;</span><br><span class="line">                w.completedTasks++;</span><br><span class="line">                w.unlock();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        completedAbruptly = <span class="literal">false</span>;</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        processWorkerExit(w, completedAbruptly);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 核心方法之一，从阻塞队列中取任务</span></span><br><span class="line"><span class="keyword">private</span> Runnable <span class="title function_">getTask</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="type">boolean</span> <span class="variable">timedOut</span> <span class="operator">=</span> <span class="literal">false</span>; <span class="comment">// Did the last poll() time out?</span></span><br><span class="line">    <span class="comment">// 死循环取任务</span></span><br><span class="line">    <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">        <span class="type">int</span> <span class="variable">c</span> <span class="operator">=</span> ctl.get();</span><br><span class="line">        <span class="type">int</span> <span class="variable">rs</span> <span class="operator">=</span> runStateOf(c);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// Check if queue empty only if necessary.</span></span><br><span class="line">        <span class="keyword">if</span> (rs &gt;= SHUTDOWN &amp;&amp; (rs &gt;= STOP || workQueue.isEmpty())) &#123;</span><br><span class="line">            decrementWorkerCount();</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="type">int</span> <span class="variable">wc</span> <span class="operator">=</span> workerCountOf(c);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 允许核心线程过期和非核心线程都可以超时取任务</span></span><br><span class="line">        <span class="type">boolean</span> <span class="variable">timed</span> <span class="operator">=</span> allowCoreThreadTimeOut || wc &gt; corePoolSize;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> ((wc &gt; maximumPoolSize || (timed &amp;&amp; timedOut))</span><br><span class="line">            &amp;&amp; (wc &gt; <span class="number">1</span> || workQueue.isEmpty())) &#123;</span><br><span class="line">            <span class="comment">// 原子性的尝试减少一个工作线程，减少成功才返回结束线程</span></span><br><span class="line">            <span class="keyword">if</span> (compareAndDecrementWorkerCount(c)) </span><br><span class="line">                <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="comment">// 如果是超时取任务，时间结束后还是取不到，则设置timedOut为true，下次循环就可以直接返回null退出了，这样，这个Worker也就终结了</span></span><br><span class="line">            <span class="type">Runnable</span> <span class="variable">r</span> <span class="operator">=</span> timed ?</span><br><span class="line">                workQueue.poll(keepAliveTime, TimeUnit.NANOSECONDS) :</span><br><span class="line">            workQueue.take();</span><br><span class="line">            <span class="comment">// 不为null才返回，就不用担心返回null而终结了当前线程</span></span><br><span class="line">            <span class="keyword">if</span> (r != <span class="literal">null</span>)</span><br><span class="line">                <span class="keyword">return</span> r;</span><br><span class="line">            timedOut = <span class="literal">true</span>;</span><br><span class="line">        &#125; <span class="keyword">catch</span> (InterruptedException retry) &#123;</span><br><span class="line">            timedOut = <span class="literal">false</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="11-3-总结"><a href="#11-3-总结" class="headerlink" title="11.3 总结"></a>11.3 总结</h2><p><strong>execute Runnable的流程</strong></p><ul><li>先判断线程池的工作线程数量是否小于核心线程数，小于核心线程数直接新建线程来执行</li><li>如果核心线程数满了，则将Runnable投入到workQueue中</li><li>如果workQueue满了，则创建非核心线程来继续执行任务</li><li>如果线程池中的工作现场数量到达了maximumPoolSize的值，则使用拒绝策略来执行任务</li></ul><p><strong>Worker的工作流程</strong></p><p>​调用getTask取任务来执行，如果取出的任务为空，则这个Worker也就结束了（终结了）。getTask不为空的话，还是先进性一系列的线程池状态校验，在执行钩子函数（beforeExecute），在真正的执行这个Runnable，再执行钩子函数（afterExecute），最后再将completedTasks加1，表示当前Worker完成的任务总数</p><p><strong>getTask流程</strong>（实现线程超时回收的关键）</p><ul><li>先进行一系列的状态校验</li><li><strong>判断是否允许超时（满足任意一个就行）</strong><ul><li><strong>allowCoreThreadTimeOut为true（都允许核心线程超时了，那没任务的情况下线程池就不该有线程）</strong></li><li><strong>当前线程池的工作线程数量大于核心线程数量就允许超时</strong></li></ul></li><li>判断是否触发减少工作线程数量的机制，然后使用CAS减少工作线程数量，减少成功才返回null，结束当前工作线程</li><li>通过阻塞队列取Runnable，如果不允许超时，则会一直阻塞到这。如果允许超时，则会超时等待keepAliveTime纳秒取Runnable，如果取不出来，则设置一次已经超时，再来循环一次，来判断是否该减少工作线程</li></ul><h1 id="12-Future"><a href="#12-Future" class="headerlink" title="12.Future"></a>12.Future</h1><p>java里Future的默认实现是FutureTask，将Callable作为构造器参数传入，就有了执行Callable的能力</p><h2 id="12-1-重点字段和主要方法："><a href="#12-1-重点字段和主要方法：" class="headerlink" title="12.1 重点字段和主要方法："></a>12.1 重点字段和主要方法：</h2><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br><span class="line">103</span><br><span class="line">104</span><br><span class="line">105</span><br><span class="line">106</span><br><span class="line">107</span><br><span class="line">108</span><br><span class="line">109</span><br><span class="line">110</span><br><span class="line">111</span><br><span class="line">112</span><br><span class="line">113</span><br><span class="line">114</span><br><span class="line">115</span><br><span class="line">116</span><br><span class="line">117</span><br><span class="line">118</span><br><span class="line">119</span><br><span class="line">120</span><br><span class="line">121</span><br><span class="line">122</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// state字段，表示了当前Future的状态，取值为如下字段</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">volatile</span> <span class="type">int</span> state;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">NEW</span>          <span class="operator">=</span> <span class="number">0</span>; <span class="comment">// 初始状态，新建</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">COMPLETING</span>   <span class="operator">=</span> <span class="number">1</span>; <span class="comment">// 正在结束</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">NORMAL</span>       <span class="operator">=</span> <span class="number">2</span>; <span class="comment">// 正常执行完毕</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">EXCEPTIONAL</span>  <span class="operator">=</span> <span class="number">3</span>; <span class="comment">// 异常执行完毕</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">CANCELLED</span>    <span class="operator">=</span> <span class="number">4</span>; <span class="comment">// 前一个状态必须是NEW，已取消（未中断）</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">INTERRUPTING</span> <span class="operator">=</span> <span class="number">5</span>; <span class="comment">// 前一个状态必须是NEW，正在中断（中断）</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">INTERRUPTED</span>  <span class="operator">=</span> <span class="number">6</span>; <span class="comment">// 取消成功的才可以设置，中断完成（中断）</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 待运行的Callable任务</span></span><br><span class="line"><span class="keyword">private</span> Callable&lt;V&gt; callable;</span><br><span class="line"><span class="comment">// Callable执行的结果。如果出现执行的过程中异常，则保存的是异常对象</span></span><br><span class="line"><span class="keyword">private</span> Object outcome;</span><br><span class="line"><span class="comment">// 运行Callable#call方法的线程（也即是运行业务代码的线程）</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">volatile</span> Thread runner;</span><br><span class="line"><span class="comment">// 当这里面的Callable还未执行完，却有其他线程调用Future#get()方法，</span></span><br><span class="line"><span class="comment">// 会将其他线程阻塞并构造为等待节点，维持一个链表结构，以便在Callable执行完毕后唤醒并回调</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">volatile</span> WaitNode waiters;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">run</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (state != NEW ||</span><br><span class="line">        !UNSAFE.compareAndSwapObject(<span class="built_in">this</span>, runnerOffset,</span><br><span class="line">                                     <span class="literal">null</span>, Thread.currentThread()))</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        Callable&lt;V&gt; c = callable;</span><br><span class="line">        <span class="keyword">if</span> (c != <span class="literal">null</span> &amp;&amp; state == NEW) &#123;</span><br><span class="line">            V result;</span><br><span class="line">            <span class="type">boolean</span> ran;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                <span class="comment">// 执行业务方法</span></span><br><span class="line">                result = c.call();</span><br><span class="line">                ran = <span class="literal">true</span>;</span><br><span class="line">            &#125; <span class="keyword">catch</span> (Throwable ex) &#123;</span><br><span class="line">                result = <span class="literal">null</span>;</span><br><span class="line">                ran = <span class="literal">false</span>;</span><br><span class="line">                setException(ex); <span class="comment">// 异常结束，将状态设为EXCEPTIONAL，如果等待队列有节点，则唤醒对应的线程</span></span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> (ran)</span><br><span class="line">                set(result); <span class="comment">// 正常结束，将状态设为NORMAL，如果等待队列有节点，则唤醒对应的线程</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        runner = <span class="literal">null</span>;</span><br><span class="line">        <span class="type">int</span> <span class="variable">s</span> <span class="operator">=</span> state;</span><br><span class="line">        <span class="keyword">if</span> (s &gt;= INTERRUPTING)</span><br><span class="line">            handlePossibleCancellationInterrupt(s);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 阻塞获取</span></span><br><span class="line"><span class="keyword">public</span> V <span class="title function_">get</span><span class="params">()</span> <span class="keyword">throws</span> InterruptedException, ExecutionException &#123;</span><br><span class="line">    <span class="type">int</span> <span class="variable">s</span> <span class="operator">=</span> state;</span><br><span class="line">    <span class="keyword">if</span> (s &lt;= COMPLETING) <span class="comment">// 还未完全结束Callabke，进入等待</span></span><br><span class="line">        s = awaitDone(<span class="literal">false</span>, <span class="number">0L</span>);</span><br><span class="line">    <span class="keyword">return</span> report(s);</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 根据state判断是否需要阻塞并做对于的事</span></span><br><span class="line"><span class="keyword">private</span> <span class="type">int</span> <span class="title function_">awaitDone</span><span class="params">(<span class="type">boolean</span> timed, <span class="type">long</span> nanos)</span></span><br><span class="line">    <span class="keyword">throws</span> InterruptedException &#123;</span><br><span class="line">    <span class="keyword">final</span> <span class="type">long</span> <span class="variable">deadline</span> <span class="operator">=</span> timed ? System.nanoTime() + nanos : <span class="number">0L</span>;</span><br><span class="line">    <span class="type">WaitNode</span> <span class="variable">q</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line">    <span class="type">boolean</span> <span class="variable">queued</span> <span class="operator">=</span> <span class="literal">false</span>;</span><br><span class="line">    <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">        <span class="keyword">if</span> (Thread.interrupted()) &#123; <span class="comment">// 当前线程支持响应中断</span></span><br><span class="line">            removeWaiter(q);</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">InterruptedException</span>();</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="type">int</span> <span class="variable">s</span> <span class="operator">=</span> state;</span><br><span class="line">        <span class="keyword">if</span> (s &gt; COMPLETING) &#123; <span class="comment">// Callable运行完毕，且result已经设置完毕</span></span><br><span class="line">            <span class="keyword">if</span> (q != <span class="literal">null</span>)</span><br><span class="line">                q.thread = <span class="literal">null</span>;</span><br><span class="line">            <span class="keyword">return</span> s;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 进入到以下分支，就代表Callable还未完全执行完毕</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (s == COMPLETING) <span class="comment">// cannot time out yet  Callable运行完毕，但正在设置result，让出执行时间，等待下次判断</span></span><br><span class="line">            Thread.<span class="keyword">yield</span>();</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (q == <span class="literal">null</span>) <span class="comment">// 第一次循环，构造等待节点</span></span><br><span class="line">            q = <span class="keyword">new</span> <span class="title class_">WaitNode</span>();</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (!queued) <span class="comment">// 还未加入等待队列，则将节点加入到等待队列中</span></span><br><span class="line">            queued = UNSAFE.compareAndSwapObject(<span class="built_in">this</span>, waitersOffset,</span><br><span class="line">                                                 q.next = waiters, q);</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (timed) &#123; <span class="comment">// 是否运行超时判断</span></span><br><span class="line">            nanos = deadline - System.nanoTime();</span><br><span class="line">            <span class="keyword">if</span> (nanos &lt;= <span class="number">0L</span>) &#123;</span><br><span class="line">                removeWaiter(q);</span><br><span class="line">                <span class="keyword">return</span> state;</span><br><span class="line">            &#125;</span><br><span class="line">            LockSupport.parkNanos(<span class="built_in">this</span>, nanos);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> <span class="comment">// 暂停当前线程，等待任务执行完毕的唤醒</span></span><br><span class="line">            LockSupport.park(<span class="built_in">this</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 任务执行完毕，唤醒等待队列的所有节点</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">finishCompletion</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="comment">// assert state &gt; COMPLETING;</span></span><br><span class="line">    <span class="keyword">for</span> (WaitNode q; (q = waiters) != <span class="literal">null</span>;) &#123;</span><br><span class="line">        <span class="keyword">if</span> (UNSAFE.compareAndSwapObject(<span class="built_in">this</span>, waitersOffset, q, <span class="literal">null</span>)) &#123;</span><br><span class="line">            <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">                <span class="type">Thread</span> <span class="variable">t</span> <span class="operator">=</span> q.thread;</span><br><span class="line">                <span class="keyword">if</span> (t != <span class="literal">null</span>) &#123;</span><br><span class="line">                    q.thread = <span class="literal">null</span>;</span><br><span class="line">                    LockSupport.unpark(t);</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="type">WaitNode</span> <span class="variable">next</span> <span class="operator">=</span> q.next;</span><br><span class="line">                <span class="keyword">if</span> (next == <span class="literal">null</span>)</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                q.next = <span class="literal">null</span>; <span class="comment">// unlink to help gc</span></span><br><span class="line">                q = next;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    done();</span><br><span class="line"></span><br><span class="line">    callable = <span class="literal">null</span>;        <span class="comment">// to reduce footprint</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="12-2-总结"><a href="#12-2-总结" class="headerlink" title="12.2 总结"></a>12.2 总结</h2><p>FutureTask根据内部的state字段来判断当前任务运行到了哪个阶段并作出对于的抉择。</p><p>​如果想获取任务执行的结果，要使用get来获取结果，get是个阻塞的方法。当任务还未执行完毕时，会将调用get的方法阻塞并构造成WaitNode，再通过内部的next字段链接下一个WaitNode，形成一个链表结构。当任务执行完毕后，内部调用的finishCompletion方法会判断等待链表是否为空，不为空就代表有线程在获取结果时被阻塞了，这时唤醒阻塞队列的所有线程，最终，调用get方法的线程返回结果。</p><h1 id="13-ScheduledThreadPoolExecutor"><a href="#13-ScheduledThreadPoolExecutor" class="headerlink" title="13. ScheduledThreadPoolExecutor"></a>13. ScheduledThreadPoolExecutor</h1><h2 id="13-1-ScheduledExecutorService"><a href="#13-1-ScheduledExecutorService" class="headerlink" title="13.1 ScheduledExecutorService"></a>13.1 ScheduledExecutorService</h2><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 创建一个一次性的延迟（定时）任务，框架中cron表达式就是通过此接口实现的</span></span><br><span class="line"><span class="comment"> * 只需要在任务完成后，在计算下一次的执行时间，再用此方法定时执行，以此类推</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> ScheduledFuture&lt;?&gt; schedule(Runnable command,</span><br><span class="line">                                   <span class="type">long</span> delay, TimeUnit unit);</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">* 执行Callable接口的任务，也是一个一次性的定时任务</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="keyword">public</span> &lt;V&gt; ScheduledFuture&lt;V&gt; <span class="title function_">schedule</span><span class="params">(Callable&lt;V&gt; callable,</span></span><br><span class="line"><span class="params">                                       <span class="type">long</span> delay, TimeUnit unit)</span>;</span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 基于固定的频率执行定时任务 </span></span><br><span class="line"><span class="comment"> * 例：初始执行任务的时间戳：当前时间戳（调用时）+ initialDelay </span></span><br><span class="line"><span class="comment"> * 第二次执行：初始执行任务开始时的时间戳 + period </span></span><br><span class="line"><span class="comment"> * 第三次执行：第二次执行任务开始时的时间戳 + period</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> ScheduledFuture&lt;?&gt; scheduleAtFixedRate(Runnable command,</span><br><span class="line">                                              <span class="type">long</span> initialDelay,</span><br><span class="line">                                              <span class="type">long</span> period,</span><br><span class="line">                                              TimeUnit unit);</span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 基于固定的周期执行定时任务 </span></span><br><span class="line"><span class="comment"> * 例：初始执行任务的时间戳：当前时间戳（调用时）+ initialDelay</span></span><br><span class="line"><span class="comment"> *  第二次执行：初始任务执行完结时的时间戳 + delay</span></span><br><span class="line"><span class="comment"> *  第三次执行：第二次任务执行完结时的时间戳 + delay</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> ScheduledFuture&lt;?&gt; scheduleWithFixedDelay(Runnable command,</span><br><span class="line">                                                 <span class="type">long</span> initialDelay,</span><br><span class="line">                                                 <span class="type">long</span> delay,</span><br><span class="line">                                                 TimeUnit unit);</span><br></pre></td></tr></table></figure><p><strong>所以，fixedRate的每次执行的开始时间是可以计算的。但fixedDelay却不行，因为它还受任务本身执行花费的时间所影响</strong>。</p><h2 id="13-2-DelayedWorkQueue"><a href="#13-2-DelayedWorkQueue" class="headerlink" title="13.2 DelayedWorkQueue"></a>13.2 DelayedWorkQueue</h2><p>数组实现的小顶堆结构，非常适合定时任务的处理</p><p><img src="/img/top_heap.png" alt="顶堆结构"></p><p>​<strong>大顶堆和小顶堆结构</strong></p><p>数组实现的小顶堆有如下规律：</p><ul><li>数组索引为0的元素是最小的</li><li>父节点小于两个子节点（但这两个子节点大小顺序不定）</li><li>数组结构紧凑（中间没有空元素）</li><li>假设<strong>父节点索引为x，则左子节点为2x + 1，右子节点为2x + 2</strong></li><li>假设<strong>子节点索引为y，则父节点索引为 (y - 1) &#x2F; 2</strong></li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 数组初始容量</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">INITIAL_CAPACITY</span> <span class="operator">=</span> <span class="number">16</span>;</span><br><span class="line"><span class="comment">// 数组实现的最小顶堆结构，queue[0]始终都是最快需要被执行的那个任务</span></span><br><span class="line"><span class="keyword">private</span> RunnableScheduledFuture&lt;?&gt;[] queue =</span><br><span class="line">    <span class="keyword">new</span> <span class="title class_">RunnableScheduledFuture</span>&lt;?&gt;[INITIAL_CAPACITY];</span><br><span class="line"><span class="keyword">private</span> <span class="keyword">final</span> <span class="type">ReentrantLock</span> <span class="variable">lock</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ReentrantLock</span>();</span><br><span class="line"><span class="keyword">private</span> <span class="type">int</span> <span class="variable">size</span> <span class="operator">=</span> <span class="number">0</span>;</span><br><span class="line"><span class="comment">// leader线程，定时等待queue[0]任务的那个线程</span></span><br><span class="line"><span class="keyword">private</span> <span class="type">Thread</span> <span class="variable">leader</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">final</span> <span class="type">Condition</span> <span class="variable">available</span> <span class="operator">=</span> lock.newCondition();</span><br><span class="line"><span class="comment">// 新加入了任务，判断时都需要上移（和父节点换个位置）并调整位置</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">siftUp</span><span class="params">(<span class="type">int</span> k, RunnableScheduledFuture&lt;?&gt; key)</span> &#123;</span><br><span class="line">    <span class="keyword">while</span> (k &gt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="type">int</span> <span class="variable">parent</span> <span class="operator">=</span> (k - <span class="number">1</span>) &gt;&gt;&gt; <span class="number">1</span>; <span class="comment">// 父节点的索引</span></span><br><span class="line">        RunnableScheduledFuture&lt;?&gt; e = queue[parent];</span><br><span class="line">        <span class="keyword">if</span> (key.compareTo(e) &gt;= <span class="number">0</span>) <span class="comment">// 当前节点的下次执行时间更久，直接break</span></span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        <span class="comment">// 当前节点的下次执行时间更快，继续递归向上遍历，直到放到合适的位置</span></span><br><span class="line">        queue[k] = e;</span><br><span class="line">        setIndex(e, k);</span><br><span class="line">        k = parent;</span><br><span class="line">    &#125;</span><br><span class="line">    queue[k] = key;</span><br><span class="line">    setIndex(key, k);</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">重写的offer方法（该方法就是线程池投递任务的方法）</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="type">boolean</span> <span class="title function_">offer</span><span class="params">(Runnable x)</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (x == <span class="literal">null</span>)</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">NullPointerException</span>();</span><br><span class="line">    RunnableScheduledFuture&lt;?&gt; e = (RunnableScheduledFuture&lt;?&gt;)x;</span><br><span class="line">    <span class="keyword">final</span> <span class="type">ReentrantLock</span> <span class="variable">lock</span> <span class="operator">=</span> <span class="built_in">this</span>.lock;</span><br><span class="line">    lock.lock();</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        <span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> size;</span><br><span class="line">        <span class="keyword">if</span> (i &gt;= queue.length)</span><br><span class="line">            grow();</span><br><span class="line">        size = i + <span class="number">1</span>;</span><br><span class="line">        <span class="keyword">if</span> (i == <span class="number">0</span>) &#123; <span class="comment">// 数组中还没有任务，直接放在首位</span></span><br><span class="line">            queue[<span class="number">0</span>] = e;</span><br><span class="line">            setIndex(e, <span class="number">0</span>);</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123; <span class="comment">// 已存在定时任务，看是否需要调整位置</span></span><br><span class="line">            siftUp(i, e);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (queue[<span class="number">0</span>] == e) &#123;<span class="comment">// 代表向队列添加了一个需要最快执行的任务</span></span><br><span class="line">            <span class="comment">// 需要重置leader线程，并将阻塞的线程从等待队列转移到同步队列，当下面的unlock后再唤醒阻塞线程</span></span><br><span class="line">            leader = <span class="literal">null</span>;</span><br><span class="line">            available.signal(); </span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        lock.unlock();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 重写了take方法</span></span><br><span class="line"><span class="keyword">public</span> RunnableScheduledFuture&lt;?&gt; take() <span class="keyword">throws</span> InterruptedException &#123;</span><br><span class="line">    <span class="keyword">final</span> <span class="type">ReentrantLock</span> <span class="variable">lock</span> <span class="operator">=</span> <span class="built_in">this</span>.lock;</span><br><span class="line">    lock.lockInterruptibly();</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">            RunnableScheduledFuture&lt;?&gt; first = queue[<span class="number">0</span>];</span><br><span class="line">            <span class="keyword">if</span> (first == <span class="literal">null</span>) <span class="comment">// 不存在定时任务，所有线程都在这等待</span></span><br><span class="line">                available.await();</span><br><span class="line">            <span class="keyword">else</span> &#123;</span><br><span class="line">                <span class="type">long</span> <span class="variable">delay</span> <span class="operator">=</span> first.getDelay(NANOSECONDS);</span><br><span class="line">                <span class="keyword">if</span> (delay &lt;= <span class="number">0</span>) <span class="comment">// 时间已过，弹出队首任务去执行它</span></span><br><span class="line">                    <span class="keyword">return</span> finishPoll(first);</span><br><span class="line">                <span class="comment">// 进入下面，无论怎样都要等待，所以直接把first置为null，下次循环再获取</span></span><br><span class="line">                <span class="comment">// 因为可能多个线程走到下面，都持有了队首的引用。避免出现RunnableScheduledFuture运行完了但不能及时回收的情况</span></span><br><span class="line">                <span class="comment">// 当然，也只有一次性的RunnableScheduledFuture才会回收，定时任务都是循环使用这个RunnableScheduledFuture的</span></span><br><span class="line">                first = <span class="literal">null</span>; <span class="comment">// don&#x27;t retain ref while waiting</span></span><br><span class="line">                <span class="keyword">if</span> (leader != <span class="literal">null</span>) <span class="comment">// 由leader存在，其他线程只需要无限期等待就行</span></span><br><span class="line">                    available.await();</span><br><span class="line">                <span class="keyword">else</span> &#123; <span class="comment">// 没有leader存在，设置当前线程为leader，并定时等待（时间就为最近待执行的那个任务的距离下次执行时间间隔）</span></span><br><span class="line">                    <span class="type">Thread</span> <span class="variable">thisThread</span> <span class="operator">=</span> Thread.currentThread();</span><br><span class="line">                    leader = thisThread;</span><br><span class="line">                    <span class="keyword">try</span> &#123;</span><br><span class="line">                        available.awaitNanos(delay); <span class="comment">// 定时等待</span></span><br><span class="line">                    &#125; <span class="keyword">finally</span> &#123; <span class="comment">// 时间一到，说明队首任务可执行了，但当前线程可能不是leader线程了，需要判断一下再置空</span></span><br><span class="line">                        <span class="keyword">if</span> (leader == thisThread) <span class="comment">// 必须判断，有可能实时的添加了一个最快需要执行的线程，导致当前线程被取消了leader</span></span><br><span class="line">                            leader = <span class="literal">null</span>;</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (leader == <span class="literal">null</span> &amp;&amp; queue[<span class="number">0</span>] != <span class="literal">null</span>)</span><br><span class="line">            <span class="comment">// 任务取出来了，leader为空且存在队首任务，需要唤醒一个无限等待的线程</span></span><br><span class="line">            <span class="comment">// 让其成为leader线程并继续定时等待</span></span><br><span class="line">            available.signal();</span><br><span class="line">        lock.unlock();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="13-3-ScheduledFutureTask"><a href="#13-3-ScheduledFutureTask" class="headerlink" title="13.3 ScheduledFutureTask"></a>13.3 ScheduledFutureTask</h2><p>ScheduledFutureTask继承了FutureTask，当向定时任务线程池投递任务时（Runnable或Callable），都会将其封装为ScheduledFutureTask</p><p>重点字段和方法</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br><span class="line">103</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/** </span></span><br><span class="line"><span class="comment"> * 当前任务的id（自增的），代表了进入队列的顺序 &lt;br/&gt;</span></span><br><span class="line"><span class="comment"> * 当两个定时任务下次执行时间一致时，sequenceNumber越小就会越早执行</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">final</span> <span class="type">long</span> sequenceNumber;</span><br><span class="line"><span class="comment">// 下次执行当前任务的纳秒时间戳</span></span><br><span class="line"><span class="keyword">private</span> <span class="type">long</span> time;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 执行定时任务的纳秒间隔时间</span></span><br><span class="line"><span class="comment"> * 大于0：代表固定的频率，不受任务的执行所花费的时间影响</span></span><br><span class="line"><span class="comment"> * 等于0：代表不是一个重复的任务（只会执行一次</span></span><br><span class="line"><span class="comment"> * 小于0：代表固定的时间间隔，基于任务执行完毕后的时间计算。（任务执行完后再基于当前时间计算下次执行时间）</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">final</span> <span class="type">long</span> period;</span><br><span class="line"><span class="comment">// 当前任务在数组中的索引</span></span><br><span class="line"><span class="type">int</span> heapIndex;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="type">long</span> <span class="title function_">getDelay</span><span class="params">(TimeUnit unit)</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> unit.convert(time - now(), NANOSECONDS);</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 比较方法，决定了放入数组的位置</span></span><br><span class="line"><span class="keyword">public</span> <span class="type">int</span> <span class="title function_">compareTo</span><span class="params">(Delayed other)</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (other == <span class="built_in">this</span>) <span class="comment">// compare zero if same object</span></span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">if</span> (other <span class="keyword">instanceof</span> ScheduledFutureTask) &#123;</span><br><span class="line">        ScheduledFutureTask&lt;?&gt; x = (ScheduledFutureTask&lt;?&gt;)other;</span><br><span class="line">        <span class="type">long</span> <span class="variable">diff</span> <span class="operator">=</span> time - x.time;</span><br><span class="line">        <span class="keyword">if</span> (diff &lt; <span class="number">0</span>)</span><br><span class="line">            <span class="keyword">return</span> -<span class="number">1</span>;</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (diff &gt; <span class="number">0</span>) <span class="comment">// 当前任务的下次执行时间更长，返回正数</span></span><br><span class="line">            <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (sequenceNumber &lt; x.sequenceNumber)</span><br><span class="line">            <span class="keyword">return</span> -<span class="number">1</span>;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">            <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="type">long</span> <span class="variable">diff</span> <span class="operator">=</span> getDelay(NANOSECONDS) - other.getDelay(NANOSECONDS);</span><br><span class="line">    <span class="keyword">return</span> (diff &lt; <span class="number">0</span>) ? -<span class="number">1</span> : (diff &gt; <span class="number">0</span>) ? <span class="number">1</span> : <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * false: 代表当前任务为一次性任务</span></span><br><span class="line"><span class="comment"> * true: 定时任务</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="type">boolean</span> <span class="title function_">isPeriodic</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> period != <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 当前任务执行完毕后，用来计算下次执行时间</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">setNextRunTime</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="type">long</span> <span class="variable">p</span> <span class="operator">=</span> period;</span><br><span class="line">    <span class="comment">// p为两次执行时间的时间间隔的纳秒值</span></span><br><span class="line">    <span class="keyword">if</span> (p &gt; <span class="number">0</span>) <span class="comment">// p大于0，即为固定时间执行的任务，基于初始运行时间计算下一次的执行时间</span></span><br><span class="line">        time += p;</span><br><span class="line">    <span class="keyword">else</span> <span class="comment">// p小于0，为基于完成任务的时间来执行，基于任务运行完的时间，来计算出下一次的执行时间</span></span><br><span class="line">        time = triggerTime(-p);</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 主方法，运行当前定时任务</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">run</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="type">boolean</span> <span class="variable">periodic</span> <span class="operator">=</span> isPeriodic();</span><br><span class="line">    <span class="keyword">if</span> (!canRunInCurrentRunState(periodic))</span><br><span class="line">        cancel(<span class="literal">false</span>);</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span> (!periodic) <span class="comment">// 非定时任务，当作普通任务直接调用FutureTask的run方法运行</span></span><br><span class="line">        ScheduledFutureTask.<span class="built_in">super</span>.run();</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span> (ScheduledFutureTask.<span class="built_in">super</span>.runAndReset()) &#123; <span class="comment">// 运行定时任务，且运行成功（没抛异常）</span></span><br><span class="line">        setNextRunTime(); <span class="comment">// 设置下一次执行时间</span></span><br><span class="line">        reExecutePeriodic(outerTask); <span class="comment">// 再把当前任务重新入队列</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">父类的FutureTask中的方法，运行并重置状态，用于任务的多次执行 </span></span><br><span class="line"><span class="comment">     * 正常执行时：不会修改运行状态（也就是说这个操作不会修改state字段值，保持初始值，以支持重复执行）。</span></span><br><span class="line"><span class="comment">     * 出现异常时：还是将state设为EXCEPTIONAL，也就是说一个定时任务要是抛出了异常，之后就不会再执行它了</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line"><span class="keyword">protected</span> <span class="type">boolean</span> <span class="title function_">runAndReset</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (state != NEW || <span class="comment">// 不为NEW状态的都不执行</span></span><br><span class="line">        !UNSAFE.compareAndSwapObject(<span class="built_in">this</span>, runnerOffset,</span><br><span class="line">                                     <span class="literal">null</span>, Thread.currentThread()))</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    <span class="type">boolean</span> <span class="variable">ran</span> <span class="operator">=</span> <span class="literal">false</span>;</span><br><span class="line">    <span class="type">int</span> <span class="variable">s</span> <span class="operator">=</span> state;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        Callable&lt;V&gt; c = callable;</span><br><span class="line">        <span class="keyword">if</span> (c != <span class="literal">null</span> &amp;&amp; s == NEW) &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                c.call(); <span class="comment">// 不设置返回结果。多次执行的任务就不该有执行结果</span></span><br><span class="line">                ran = <span class="literal">true</span>;</span><br><span class="line">            &#125; <span class="keyword">catch</span> (Throwable ex) &#123;</span><br><span class="line">                setException(ex);  <span class="comment">// 抛出异常，修改state为EXCEPTIONAL，以后也不在执行它</span></span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        <span class="comment">// runner must be non-null until state is settled to</span></span><br><span class="line">        <span class="comment">// prevent concurrent calls to run()</span></span><br><span class="line">        runner = <span class="literal">null</span>;</span><br><span class="line">        <span class="comment">// state must be re-read after nulling runner to prevent</span></span><br><span class="line">        <span class="comment">// leaked interrupts</span></span><br><span class="line">        s = state;</span><br><span class="line">        <span class="keyword">if</span> (s &gt;= INTERRUPTING)</span><br><span class="line">            handlePossibleCancellationInterrupt(s);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> ran &amp;&amp; s == NEW;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="13-4-总结"><a href="#13-4-总结" class="headerlink" title="13.4 总结"></a>13.4 总结</h2><p><a href="https://segmentfault.com/a/1190000038371064">优秀的文章</a></p><p>​ScheduledThreadPoolExecutor本质还是个线程池，<strong>内部的DelayedWorkQueue就是工作队列。投递的定时任务和普通任务都会封装为ScheduledFutureTask，并最终放入DelayedWorkQueue里的那个数组（只不过定时任务有延时，可能会放在队列中的任何位置。而普通任务封装的ScheduledFutureTask执行时间就是当前而已，始终会放到队列的队首并立马执行）</strong></p><p>​DelayedWorkQueue实现了BlockingQueue，是基于<strong>数组的最小顶堆的数据结构</strong>实现，以此<strong>保证数组的第一个位置就是最近需要被执行的任务</strong>。结构图和特点如下</p><p>​ScheduledThreadPoolExecutor还使用了<strong>Leader-Follower模式</strong>，leader线程定时等待工作队列中第一个任务，其余线程一般就都无限期等待（如果向工作队列添加的是一个最快需要被执行的任务，可能就有多个定时等待的线程，但leader线程始终都会是最快需要被执行任务的线程）。</p><p><strong>为什么使用Leader-Follower模式：</strong></p><blockquote><p>​避免资源的浪费。定时任务再怎么排序，也只会有一个是最快需要执行的任务（时间相同会根据sequenceNumber排序），只需要设计一个定时等待线程等待这个最快需要执行的任务。当这个最快需要执行的任务触发后，再设计一个新的leader线程等待下一个最近的定时任务。理想的情况下，定时任务线程池只会有一个定时等待的线程（Leader线程），其余线程要么正在运行定时任务，要么全部无限期阻塞（Follower线程），最大程度的避免资源浪费（无限期等待的线程不用想其它的，乖乖等待被其他线程唤醒就行。而定时等待的线程需要在时间到达后被唤醒，至少需要被定时器监视以用来执行唤醒操作）</p></blockquote><ul><li><strong>固定周期</strong>：受执行时常影响，只有当任务结束后才相对于结束时间来计算任务的下次执行时间</li><li><strong>固定频率</strong>：不受任务的执行时常所影响，当任务投递到队列时就可以预判到以后任何执行该任务的时间</li></ul><p>​一个被投递的周期任务首先会封装成ScheduledFutureTask，再根据其下次执行时间放在DelayedWorkQueue的某个位置。如果放在了DelayedWorkQueue的队首，则使用定时任务线程池里的线程超时等待，以便时间到达后开始执行。正常执行完毕则会先根据其是固定周期任务还是固定频率的任务来计算下次执行时间并修赋值到ScheduledFutureTask的time字段，再将这个任务再次入队列，这样递归去执行。执行中如果抛出了异常，则会将ScheduledFutureTask的state修改为异常，之后就不再执行这个任务了</p><h1 id="14-CopyOnWriteArrayList"><a href="#14-CopyOnWriteArrayList" class="headerlink" title="14. CopyOnWriteArrayList"></a>14. CopyOnWriteArrayList</h1><p>​线程安全的List，写时复制List，顾名思义，就是在修改元素时（添加，删除等操作），使用新的数组来添加或删除，原内部数组在设置后就再也不会修改内部的数据结构了。</p><p>​所以，这种数据结构<strong>适合用在很少修改元素，大量读取元素的多线程并发操作的场合</strong></p><p>主要字段和方法</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 数据修改时用到的互斥锁</span></span><br><span class="line"><span class="keyword">final</span> <span class="keyword">transient</span> <span class="type">ReentrantLock</span> <span class="variable">lock</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ReentrantLock</span>();</span><br><span class="line"></span><br><span class="line"><span class="comment">// 数组实现，并使用volatile保持多线程对array的可见性</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">transient</span> <span class="keyword">volatile</span> Object[] array;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 添加元素</span></span><br><span class="line"><span class="keyword">public</span> <span class="type">boolean</span> <span class="title function_">add</span><span class="params">(E e)</span> &#123;</span><br><span class="line">    <span class="keyword">final</span> <span class="type">ReentrantLock</span> <span class="variable">lock</span> <span class="operator">=</span> <span class="built_in">this</span>.lock;</span><br><span class="line">    lock.lock(); <span class="comment">// 加互斥锁</span></span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        <span class="comment">// 原数组不会修改，每次都新建一个数组，在将array字段替换为新数组</span></span><br><span class="line">        Object[] elements = getArray();</span><br><span class="line">        <span class="type">int</span> <span class="variable">len</span> <span class="operator">=</span> elements.length;</span><br><span class="line">        Object[] newElements = Arrays.copyOf(elements, len + <span class="number">1</span>);</span><br><span class="line">        newElements[len] = e;</span><br><span class="line">        setArray(newElements);</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        lock.unlock();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>Hello World</title>
      <link href="/2025/01/06/hello-world/"/>
      <url>/2025/01/06/hello-world/</url>
      
        <content type="html"><![CDATA[<p>Welcome to <a href="https://hexo.io/">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues">GitHub</a>.</p><h2 id="Quick-Start"><a href="#Quick-Start" class="headerlink" title="Quick Start"></a>Quick Start</h2><h3 id="Create-a-new-post"><a href="#Create-a-new-post" class="headerlink" title="Create a new post"></a>Create a new post</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ hexo new <span class="string">&quot;My New Post&quot;</span></span><br></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/writing.html">Writing</a></p><h3 id="Run-server"><a href="#Run-server" class="headerlink" title="Run server"></a>Run server</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ hexo server</span><br></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/server.html">Server</a></p><h3 id="Generate-static-files"><a href="#Generate-static-files" class="headerlink" title="Generate static files"></a>Generate static files</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ hexo generate</span><br></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/generating.html">Generating</a></p><h3 id="Deploy-to-remote-sites"><a href="#Deploy-to-remote-sites" class="headerlink" title="Deploy to remote sites"></a>Deploy to remote sites</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ hexo deploy</span><br></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/one-command-deployment.html">Deployment</a></p>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title></title>
      <link href="/2025/01/06/g1/"/>
      <url>/2025/01/06/g1/</url>
      
        <content type="html"><![CDATA[<h1 id="G1"><a href="#G1" class="headerlink" title="G1"></a>G1</h1><p>用了G1，就不需要在指定新生代和老年代的的大小或比例关系了</p><p>大对象：对象大小超过一个Region容量一半的对象</p><ul><li><strong>-XX:+UseG1GC</strong></li><li><strong>-XX:G1HeapRegionSize&#x3D;2M</strong>：一个Region的大小</li><li><strong>-XX:MaxGCPauseMillis&#x3D;80</strong>：允许收集停顿的最大时常（毫秒）</li></ul><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><span class="line">2020-11-23T11:40:46.167+0800: 1.503: [GC pause (G1 Evacuation Pause) (young), 0.0048336 secs]</span><br><span class="line">   // 下面的Min,Avg,Max,Diff,Sum分别表示GC线程最小启动或耗时时间（后面的也是），平均，最大，最大差值，和总耗时，单位都为ms</span><br><span class="line">   [Parallel Time: 3.1 ms, GC Workers: 6] // 本次YGC共6个GC线程，总耗时3.1ms</span><br><span class="line">      [GC Worker Start (ms): Min: 1503.0, Avg: 1504.5, Max: 1506.1, Diff: 3.0] // 本次GC线程启动（相对于JVM的启动）</span><br><span class="line">      [Ext Root Scanning (ms): Min: 0.0, Avg: 0.2, Max: 0.7, Diff: 0.7, Sum: 1.1] // 本次GC线程的Root集合扫描时间</span><br><span class="line">      [Update RS (ms): Min: 0.0, Avg: 0.0, Max: 0.0, Diff: 0.0, Sum: 0.0] // 更新Remember Sets 的耗时统计信息（记忆集一般使用来解决跨Region的引用）</span><br><span class="line">         [Processed Buffers: Min: 0, Avg: 0.0, Max: 0, Diff: 0, Sum: 0]</span><br><span class="line">      [Scan RS (ms): Min: 0.0, Avg: 0.0, Max: 0.0, Diff: 0.0, Sum: 0.0] // 每个Region都会有一个RSet，RSet又包含指向这个Region的Cards引用，这个阶段就是扫描RSet中的Cards，从而分辨出Eden哪些对象被老年代引用，从而这些不会被GC</span><br><span class="line">      [Code Root Scanning (ms): Min: 0.0, Avg: 0.0, Max: 0.2, Diff: 0.2, Sum: 0.2] // 扫描代码中的root节点（局部变量）</span><br><span class="line">      [Object Copy (ms): Min: 0.0, Avg: 1.3, Max: 2.7, Diff: 2.7, Sum: 7.6] // 对象copy，将存活的对象copy到目标Region中</span><br><span class="line">      [Termination (ms): Min: 0.0, Avg: 0.0, Max: 0.1, Diff: 0.1, Sum: 0.3]</span><br><span class="line">         [Termination Attempts: Min: 1, Avg: 1.2, Max: 2, Diff: 1, Sum: 7]</span><br><span class="line">      [GC Worker Other (ms): Min: 0.0, Avg: 0.0, Max: 0.0, Diff: 0.0, Sum: 0.2] // GC线程完成其他任务的时间</span><br><span class="line">      [GC Worker Total (ms): Min: 0.0, Avg: 1.6, Max: 3.1, Diff: 3.1, Sum: 9.4] // GC线程整个生命周期总计消耗时间</span><br><span class="line">      [GC Worker End (ms): Min: 1506.1, Avg: 1506.1, Max: 1506.1, Diff: 0.0] // GC线程完成任务的停止时间（相对于JVM）</span><br><span class="line">   [Code Root Fixup: 0.0 ms]</span><br><span class="line">   [Code Root Purge: 0.0 ms]</span><br><span class="line">   [Clear CT: 0.1 ms] // 清理Card Table（卡表）</span><br><span class="line">   [Other: 1.6 ms]</span><br><span class="line">      [Choose CSet: 0.0 ms] // 选择要回收的Region放入CSet（会根据停顿时间来决定）</span><br><span class="line">      [Ref Proc: 1.4 ms] // 处理引用对象耗时时间（Weak、Soft、Phantom、JNI等等）</span><br><span class="line">      [Ref Enq: 0.0 ms] // 遍历所有引用，将不能回收的放入pending列表</span><br><span class="line">      [Redirty Cards: 0.0 ms] // 重置card为dirty</span><br><span class="line">      // 大型对象的回收</span><br><span class="line">      [Humongous Register: 0.0 ms] </span><br><span class="line">      [Humongous Reclaim: 0.0 ms]</span><br><span class="line">      [Free CSet: 0.0 ms] // 释放CSet中Region占用的内存空间所耗时间</span><br><span class="line">   [Eden: 51.0M(51.0M)-&gt;0.0B(46.0M) Survivors: 0.0B-&gt;5120.0K Heap: 51.0M(1024.0M)-&gt;4815.7K(1024.0M)]</span><br><span class="line"> [Times: user=0.05 sys=0.00, real=0.01 secs] </span><br></pre></td></tr></table></figure>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title></title>
      <link href="/2025/01/06/jvm-chang-yong-can-shu-zheng-li/"/>
      <url>/2025/01/06/jvm-chang-yong-can-shu-zheng-li/</url>
      
        <content type="html"><![CDATA[<h1 id="JVM"><a href="#JVM" class="headerlink" title="JVM"></a>JVM</h1><h2 id="1-jvm参数整理"><a href="#1-jvm参数整理" class="headerlink" title="1.jvm参数整理"></a>1.jvm参数整理</h2><ul><li><strong>-Xms64m</strong> ：初始堆大小</li><li><strong>-Xmx128m</strong> ：最大堆大小</li><li><strong>-Xmn32m</strong> ：年轻代大小</li><li><strong>-XX:MaxNewSize&#x3D;256m</strong> : 最大新生代大小</li><li><strong>-Xss512k</strong>：栈大小</li><li><strong>-XX:MetaspaceSize&#x3D;256M</strong> ：Metaspace扩容时触发FullGC的初始化阈值(并不是元空间的初始化大小，元空间是不断扩容的，当达到这个值时，就会触发full gc，<a href="https://www.jianshu.com/p/b448c21d2e71">链接</a>）</li><li><strong>-XX:MaxMetaspaceSize&#x3D;512M</strong>：Metaspace最大大小</li><li><strong>-XX:NewRatio&#x3D;2</strong>：老年代和新生代的比例</li><li><strong>-XX:SurvivorRatio&#x3D;8</strong> ：Eden区与一个Survivor区的大小比值（所以<strong>s0:s1:eden&#x3D;1:1:8</strong>）</li><li><strong>-XX:MinHeapFreeRatio&#x3D;40</strong>：空闲堆空间的最小百分比。如果空闲堆空间的比例小于它，则会进行堆扩容</li><li><strong>-XX:MaxHeapFreeRatio&#x3D;70</strong>：空闲堆空间的最大百分比。如果空闲堆空间的比例大于它，则会进行堆缩容</li><li><strong>-XX:-DisableExplicitGC</strong>：禁止显式GC，即禁止程序中System.gc()。个人感觉没必要</li><li><strong>-XX:+HeapDumpOnOutOfMemoryError</strong>：OOM时导出堆快照到文件</li><li><strong>-XX:HeapDumpPath&#x3D;&#x2F;home&#x2F;huskie&#x2F;gc&#x2F;oom.hprof</strong>：OOM时导出文件路径</li><li><strong>-Xloggc:</strong>&#x2F;home&#x2F;ruidong&#x2F;gc.log   gc存储日志</li><li><strong>-XX:OnOutOfMemoryError</strong>：OOM时操作，比如如执行脚本发送邮件</li><li><strong>-XX:+TraceClassLoading</strong>：打印加载类的详细信息</li><li><strong>-XX:+PrintGCDetails</strong>：打印GC详细信息</li><li><strong>-XX:+PrintGCTimeStamps</strong>：打印CG发生的时间戳（相对于项目启动时间）</li><li><strong>-XX:+PrintGCDateStamps</strong>：打印GC发生的时间</li><li><strong>-XX:+PrintHeapAtGC</strong>：每一次GC前和GC后，都打印堆信息</li><li><strong>-XX:+PrintClassHistogram</strong>：按下Ctrl+Break后，打印类的信息</li><li><strong>-XX:+PrintGCApplicationConcurrentTime</strong> ：打印应用程序的运行时间（许多事情会导致JVM暂停所有线程，停在安全点。gc也只是其中的一种，当暂停之后在重启应用线程，则会刷新这个时间（归0），在重新计数）<a href="http://ifeve.com/logging-stop-the-world-pauses-in-jvm/">链接</a></li><li><strong>-XX:+PrintGCApplicationStoppedTime</strong> ：打印应用线程暂停的时间，显示应用线程被暂停了多久和应用线程暂停到安全点花费的时间</li><li><strong>-XX:TargetSurvivorRatio&#x3D;50</strong> ：survivor空间的晋升大小空间百分比（默认为50）</li><li><strong>-XX:MaxTenuringThreshold&#x3D;15</strong>  ：年轻代晋升到老年代的最大年龄阈值(tenuring threshold)。默认值为 15[每次GC，增加1岁，到15岁如果还要存活，放入Old区]。<strong>jvm还会动态的计算晋升阈值，方法：依次从年龄为1的对象大小加起来，一直加到大小超过了 [（TargetSurvivorRatio * survivor_capacity）&#x2F; 100 ]值，这时加起来的最大年龄大小即为这次晋升的临界阈值</strong>（具体算法在：hotspot\src\share\vm\gc_implementation\shared\ageTable.cpp文件里，方法为compute_tenuring_threshold）</li><li><strong>-XX:+PrintTenuringDistribution</strong> ：ygc 时打印当前晋升年龄信息</li></ul><h2 id="2-垃圾收集器"><a href="#2-垃圾收集器" class="headerlink" title="2.垃圾收集器"></a>2.垃圾收集器</h2><h3 id="2-1-新生代"><a href="#2-1-新生代" class="headerlink" title="2.1 新生代"></a>2.1 新生代</h3><h4 id="2-1-1-Serial（hotspot虚拟机在客户端下的默认新生代垃圾收集器）"><a href="#2-1-1-Serial（hotspot虚拟机在客户端下的默认新生代垃圾收集器）" class="headerlink" title="2.1.1 Serial（hotspot虚拟机在客户端下的默认新生代垃圾收集器）"></a>2.1.1 Serial（hotspot虚拟机在客户端下的默认新生代垃圾收集器）</h4><p>单线程新生代收集器，复制算法，整个过程STW</p><p>优势：内存消耗最小</p><p>缺点：不适合大内存多处理器工作，慢</p><h4 id="2-1-2-ParNew"><a href="#2-1-2-ParNew" class="headerlink" title="2.1.2 ParNew"></a>2.1.2 ParNew</h4><p>多线程并行的新生代收集器，复制算法，整个过程STW</p><ul><li><strong>-XX:ParallelGCThreads&#x3D;4</strong> ：并行收集的线程数</li></ul><h4 id="2-1-3-Parallel-Scavenge"><a href="#2-1-3-Parallel-Scavenge" class="headerlink" title="2.1.3  Parallel Scavenge"></a>2.1.3  Parallel Scavenge</h4><ul><li>吞吐量 &#x3D; 运行用户代码时间 &#x2F; ( 运行用户代码时间 + 垃圾收集时间 )</li></ul><p>吞吐量优先的新生代并行多线程收集器，复制算法（<strong>标记-复制</strong>算法）</p><p>三个重要参数：</p><ul><li><strong>XX:MaxGCPauseMillis</strong> ：垃圾收集最大停顿时间，大于0的毫秒数</li><li><strong>-XX:GCTimeRatio</strong>: 大于0小于100的整数（运行用户代码时间比上垃圾回收的时间），默认为99，即允许最大1%的垃圾回收时间</li><li><strong>-XX:+UseAdaptiveSizePolicy</strong>：开启垃圾收集器的自适应调节策略。虚拟机动态调整新生代，Eden区，Survivor区的比例和晋升大小</li></ul><h3 id="2-2-老年代"><a href="#2-2-老年代" class="headerlink" title="2.2 老年代"></a>2.2 老年代</h3><h4 id="2-2-1-CMS"><a href="#2-2-1-CMS" class="headerlink" title="2.2.1 CMS"></a>2.2.1 CMS</h4><p><strong>标记-清除</strong>算法的老年代收集器</p><h4 id="2-2-2-Serial-Old"><a href="#2-2-2-Serial-Old" class="headerlink" title="2.2.2 Serial Old"></a>2.2.2 Serial Old</h4><p>Serial的老年代会收集，<strong>标记-整理</strong>算法</p><h4 id="2-2-3-Parallel-Scavenge-Old"><a href="#2-2-3-Parallel-Scavenge-Old" class="headerlink" title="2.2.3 Parallel Scavenge Old"></a>2.2.3 Parallel Scavenge Old</h4><p>Parallel Scavenge收集器的老年代版本，标记-整理算法</p><h3 id="2-3-整堆"><a href="#2-3-整堆" class="headerlink" title="2.3 整堆"></a>2.3 整堆</h3><h4 id="2-3-1-G1"><a href="#2-3-1-G1" class="headerlink" title="2.3.1 G1"></a>2.3.1 G1</h4><p>garbage-frist收集器</p><h3 id="2-4-垃圾收集器组合"><a href="#2-4-垃圾收集器组合" class="headerlink" title="2.4  垃圾收集器组合"></a>2.4  垃圾收集器组合</h3><h4 id="2-4-1-Serial-SerialOld"><a href="#2-4-1-Serial-SerialOld" class="headerlink" title="2.4.1 Serial + SerialOld"></a>2.4.1 Serial + SerialOld</h4><h4 id="2-4-2-Serial-CMS-jdk8声明废弃，jdk9已被取消"><a href="#2-4-2-Serial-CMS-jdk8声明废弃，jdk9已被取消" class="headerlink" title="2.4.2 Serial + CMS (jdk8声明废弃，jdk9已被取消)"></a>2.4.2 Serial + CMS (jdk8声明废弃，jdk9已被取消)</h4><h4 id="2-4-3-ParNew-CMS-（使用CMS收集器的默认组合"><a href="#2-4-3-ParNew-CMS-（使用CMS收集器的默认组合" class="headerlink" title="2.4.3 ParNew +CMS （使用CMS收集器的默认组合)"></a>2.4.3 ParNew +CMS （使用CMS收集器的默认组合)</h4><h4 id="2-4-4-ParNew-SerialOld-jdk8声明废弃，jdk9已被取消"><a href="#2-4-4-ParNew-SerialOld-jdk8声明废弃，jdk9已被取消" class="headerlink" title="2.4.4 ParNew + SerialOld (jdk8声明废弃，jdk9已被取消)"></a>2.4.4 ParNew + SerialOld (jdk8声明废弃，jdk9已被取消)</h4><h4 id="2-4-5-Parallel-Scavenge-SerialOld"><a href="#2-4-5-Parallel-Scavenge-SerialOld" class="headerlink" title="2.4.5 Parallel Scavenge +  SerialOld"></a>2.4.5 Parallel Scavenge +  SerialOld</h4><h4 id="2-4-6-Parallel-Scavenge-Parallel-Scavenge-Old（jdk8的默认组合）"><a href="#2-4-6-Parallel-Scavenge-Parallel-Scavenge-Old（jdk8的默认组合）" class="headerlink" title="2.4.6 Parallel Scavenge + Parallel Scavenge Old（jdk8的默认组合）"></a>2.4.6 Parallel Scavenge + Parallel Scavenge Old（jdk8的默认组合）</h4><h4 id="2-4-7-G1（jdk9的默认收集器，且CMS被标记为废弃了）"><a href="#2-4-7-G1（jdk9的默认收集器，且CMS被标记为废弃了）" class="headerlink" title="2.4.7 G1（jdk9的默认收集器，且CMS被标记为废弃了）"></a>2.4.7 G1（jdk9的默认收集器，且CMS被标记为废弃了）</h4><h2 id="3-tips"><a href="#3-tips" class="headerlink" title="3.tips"></a>3.tips</h2><ul><li><strong>java -XX:+PrintFlagsFinal -version</strong>  ：查看jvm默认参数。数据太多可配合grep使用</li></ul><h2 id="4-标记对象时-并发的可达性分析"><a href="#4-标记对象时-并发的可达性分析" class="headerlink" title="4. 标记对象时-并发的可达性分析"></a>4. 标记对象时-并发的可达性分析</h2><p>黑色对象：存活对象，且这个对象的全部引用都扫描过（代表不需要再次扫描了）</p><p>灰色对象：存活对象，但这个对象至少存在一个引用未被扫描（它持有的引用没扫描完）</p><p>白色对象：不可达对象，不会被扫描到</p><h4 id="1-问题：对象消失产生的原因（二者缺一不可）"><a href="#1-问题：对象消失产生的原因（二者缺一不可）" class="headerlink" title="1.问题：对象消失产生的原因（二者缺一不可）"></a>1.问题：对象消失产生的原因（二者缺一不可）</h4><blockquote><ol><li>黑色对象重新引用了白色对象</li><li>灰色对象删除了持有的该白色对象的全部引用</li></ol><p>为什么二者缺一不可：首先第一条，既然是重新引用了白色对象，那么代表这个白色对象一定是被引用在灰色对象上的，且这个灰色对象还没扫描到这个白色对象。第二，考虑到扫描灰色对象的工作还没做完，必须在做完前删除掉引用的这个白色对象</p></blockquote><h4 id="2-解决方式（二选一）"><a href="#2-解决方式（二选一）" class="headerlink" title="2.解决方式（二选一）"></a>2.解决方式（二选一）</h4><h5 id="1-增量更新（CMS收集器使用）"><a href="#1-增量更新（CMS收集器使用）" class="headerlink" title="1.增量更新（CMS收集器使用）"></a>1.增量更新（CMS收集器使用）</h5><p>gc线程如果已经扫描完成A对象（黑色），用户线程在这期间又将A对象持有了一个应该被回收的B对象引用（白色）。此时需要记录A对象，将其从黑变为灰，在gc线程扫描完成后再扫描一次A对象</p><h5 id="2-原始快照"><a href="#2-原始快照" class="headerlink" title="2.原始快照"></a>2.原始快照</h5><p>记录删除了白色对象引用的灰色对象，gc线程第一次扫描结束后在扫描这个灰色对象</p><h2 id="5-GCRoots对象"><a href="#5-GCRoots对象" class="headerlink" title="5 GCRoots对象"></a>5 GCRoots对象</h2><ul><li>虚拟机栈(栈帧中的本地变量表)中引用的对象</li><li>本地方法栈(Native 方法)中引用的对象</li><li>方法区中类静态属性引用的对象</li><li>方法区中常量引用的对象</li><li>所有被同步锁持有的对象</li></ul>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title></title>
      <link href="/2025/01/06/seata-de-at-mo-shi-fen-xi/"/>
      <url>/2025/01/06/seata-de-at-mo-shi-fen-xi/</url>
      
        <content type="html"><![CDATA[<h1 id="Seata的AT模式分析-1-5-0版本"><a href="#Seata的AT模式分析-1-5-0版本" class="headerlink" title="Seata的AT模式分析(1.5.0版本)"></a>Seata的AT模式分析(1.5.0版本)</h1><h2 id="1-增强机制"><a href="#1-增强机制" class="headerlink" title="1 增强机制"></a>1 增强机制</h2><p>SeataAutoConfiguration中的GlobalTransactionScanner，主要用来代理@GlobalTransactional注解和客户端RM和TM的注册</p><h3 id="1-1-GlobalTransactionScanner的实例化"><a href="#1-1-GlobalTransactionScanner的实例化" class="headerlink" title="1.1 GlobalTransactionScanner的实例化"></a>1.1 GlobalTransactionScanner的实例化</h3><h4 id="1-1-1-bean的自动代理"><a href="#1-1-1-bean的自动代理" class="headerlink" title="1.1.1 bean的自动代理"></a>1.1.1 bean的自动代理</h4><p>​<strong>GlobalTransactionScanner继承了AbstractAutoProxyCreator抽象类，具有了自动代理bean的功能</strong>。作为一个BeanPostProcessor，将在postProcessAfterInitialization方法调用时判断该bean是否应该被代理（对于AT模式，就是当前bean的class或method必须有@GlobalTransactional或@GlobalLock注解），最终被GlobalTransactionalInterceptor拦截。</p><h4 id="1-1-2-初始化"><a href="#1-1-2-初始化" class="headerlink" title="1.1.2 初始化"></a>1.1.2 初始化</h4><p>​<strong>GlobalTransactionScanner也实现了InitializingBean接口，在afterPropertiesSet方法中，将会对TM和RM进行初始化</strong></p><h5 id="1-1-2-1-TM初始化"><a href="#1-1-2-1-TM初始化" class="headerlink" title="1.1.2.1 TM初始化"></a>1.1.2.1 TM初始化</h5><ul><li><p>实例化TmNettyRemotingClient对象（会创建好netty客户端的Bootstrap相关配置，等待后续init使用）</p></li><li><p>开始<strong>初始化TmNettyRemotingClient，先注册一些ResponseProcessor，用来处理server端回传的respons</strong>e（因为时TM，所以只会注册和全局事务的begin，commit，rolback等相关response的Processor，具体类型如下）</p><ul><li><pre class="line-numbers language-language-txt"><code class="language-language-txt">MessageType.TYPE_SEATA_MERGE_RESULTMessageType.TYPE_GLOBAL_BEGIN_RESULTMessageType.TYPE_GLOBAL_COMMIT_RESULTMessageType.TYPE_GLOBAL_REPORT_RESULTMessageType.TYPE_GLOBAL_ROLLBACK_RESULTMessageType.TYPE_GLOBAL_STATUS_RESULTMessageType.TYPE_REG_CLT_RESULT<figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"></span><br><span class="line">- 开启一些定时任务，包括与服务端Channel的reconnect任务、**MergedSend任务（处理批量消息发送）**、future的超时检测任务。</span><br><span class="line"></span><br><span class="line">- 启动和TM相关的netty客户端的Bootstrap，添加对应的ChannelHandler，包括IdleStateHandler（心跳检测，会进行ping-pong操作）、解码和编码器、**ClientHandler（双向handler，开始真正处理数据，同时也处理IdleStateHandler发来的IdleStateEvent事件，判断后是否进行ping-pong，以此维护长连接）**</span><br><span class="line"></span><br><span class="line">##### 1.1.2.2 RM初始化</span><br><span class="line"></span><br><span class="line">- 实例化RmNettyRemotingClient对象，和上面类似，也会创建好netty客户端的Bootstrap相关配置</span><br><span class="line"></span><br><span class="line">- 开始**初始化RmNettyRemotingClient，也会先注册一些ResponseProcessor，用来处理server端回传的response**（因为时RM，所以只会注册资源（可以理解成数据库）相关的操作，具体类型如下）</span><br><span class="line"></span><br><span class="line">  - ```java</span><br><span class="line">    MessageType.TYPE_BRANCH_COMMIT</span><br><span class="line">    MessageType.TYPE_BRANCH_ROLLBACK</span><br><span class="line">    MessageType.TYPE_RM_DELETE_UNDOLOG</span><br><span class="line">    </span><br><span class="line">    MessageType.TYPE_SEATA_MERGE_RESUL</span><br><span class="line">    MessageType.TYPE_BRANCH_REGISTER_RESULT</span><br><span class="line">    MessageType.TYPE_BRANCH_STATUS_REPORT_RESULT</span><br><span class="line">    MessageType.TYPE_GLOBAL_LOCK_QUERY_RESULT</span><br><span class="line">    MessageType.TYPE_REG_RM_RESULT</span><br></pre></td></tr></table></figure><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li></ul></li><li><p>和上面一样，开启RM相关的netty bootstrap并注册相同的ChannelHandler</p></li></ul><h3 id="1-2-相关表"><a href="#1-2-相关表" class="headerlink" title="1.2 相关表"></a>1.2 相关表</h3><ul><li><strong>undo_log</strong>：客户端的表，保存修改数据sql操作前后镜像值，主要用于全局事务回滚来构造回滚sql以回滚数据</li><li><strong>global_session</strong>：服务端表，每一条记录都代表一个全局事务</li><li><strong>branch_session</strong>：服务端表，每一条记录都代表一个从客户端注册的分支事务</li><li><strong>lock_table</strong>：服务端表，客户端在全局事务期间修改的数据，每一条记录了某个数据库每个表的具体某个行的主键值，用在多个全局事务期间解决写冲突。</li></ul><h2 id="2-重要术语"><a href="#2-重要术语" class="headerlink" title="2 重要术语"></a>2 重要术语</h2><h3 id="2-1-RM（Resource-Manager）"><a href="#2-1-RM（Resource-Manager）" class="headerlink" title="2.1 RM（Resource Manager）"></a>2.1 RM（Resource Manager）</h3><p>​资源管理器，管理分支事务处理的资源，与TC交谈以注册分支事务和报告分支事务的状态，并驱动分支事务提交或回滚。</p><p>​<strong>作用在seata客户端，代码实现中和DatsSource的代理绑定，负责和数据库相关的操作打交道，默认实现为DefaultResourceManager（仅是一个适配器，针对不同的模式适配了不同的ResourceManager实现。比如AT模式的默认实现是DataSourceManager）</strong>。在Connection的commit中，负责向server端注册当前分支事务；在Connection的rollback中，负责向server端报告当前分支事务的错误状态。同时也处理服务端发送过来的分支事务commit（删除undo_log）或rollback（生成补偿sql并执行）操作。</p><h3 id="2-2-TM（Transaction-Manager）"><a href="#2-2-TM（Transaction-Manager）" class="headerlink" title="2.2 TM（Transaction Manager）"></a>2.2 TM（Transaction Manager）</h3><p>​ 事务管理器，开始全局事务、提交或回滚全局事务。</p><p>​<strong>作用在seata客户端，代码实现中和@GlobalTransactional的拦截打交道，，默认实现为DefaultTransactionManager。负责开启、commit或rollback全局事务（commit和rollback操作只是向server发起这个请求了，具体的还需要TC和RM来执行真正的数据处理）</strong>。三个核心方法如下</p><ul><li><strong>begin</strong>：向server发送开启全局事务的请求。<strong>server端会创建一条global_session数据来表示这个全局事务</strong>，并回传给TM对应的xid</li><li><strong>commit</strong>：向server端发起全局事务的commit。server端具体做法如下（<strong>从第二个操作开始，就会异步处理了，因为这之后的操作基本就是删除些用来支持全局事务的数据，不会再对分支事务修改的数据有影响了</strong>）。<strong>所以，commit操作是很快的，TM只需等待TC删除掉锁信息即可</strong><ul><li>修改global_session状态，<strong>释放锁资源（即删除lock_table对应的记录）</strong></li><li>拿到这个global_session关联的branch_session，依次<strong>触发branch_session的commit（即发送请求到RM，RM会删除掉branch_session对应的undo_log数据）</strong></li><li><strong>server端删除对应的branch_session记录</strong></li><li>全部branch_session操作完成后，TC开始修改global_session状态为完毕然后<strong>删除这个global_session</strong></li><li>返回对应的GlobalStatus状态给TM。至此，全局事务commit完毕</li></ul></li><li><strong>rollback</strong>：向server端发起全局事务的rollback。server端具体做法如下<ul><li>设置对应的global_session状态为关闭，避免新分支的注册</li><li>拿到这个global_session关联的branch_session，依次<strong>触发branch_session的rollback（即发送请求到RM，RM会拿出这个branch_session对应的undo_log数据，并进行undo操作，复原数据）</strong></li><li><strong>server端删除对应的branch_session记录</strong></li><li><strong>全部branch_session操作rollback成功后，TC再释放锁资源（即删除lock_table对应的记录），并删除对应的global_session数据</strong></li><li>返回对应的GlobalStatus状态给TM。至此，全局事务rollback完毕</li></ul></li></ul><h3 id="2-3-TC（Transaction-Coordinator）"><a href="#2-3-TC（Transaction-Coordinator）" class="headerlink" title="2.3 TC（Transaction Coordinator）"></a>2.3 TC（Transaction Coordinator）</h3><p>​事务协调者，作用在seata服务端，维护全局和分支事务的状态，驱动全局事务提交或回滚，<strong>默认实现为DefaultCoordinator</strong>。</p><p>​与RM打交道，实现分支事务的注册等，并在TM发起全局事务的commit和rollback后再驱动分支事务的commit和rollback。</p><p>​和TM打交道，实现全局事务的开启、commit和rollback等操作</p><h3 id="2-4-全局事务的发起者"><a href="#2-4-全局事务的发起者" class="headerlink" title="2.4 全局事务的发起者"></a>2.4 全局事务的发起者</h3><p>​<strong>全局事务的起点，微服务调用链走到@GlobalTransactional时，线程上线文不存在xid（有可能是第一次发起，也有可能时事务传播策略导致新建的全局事务等）。类比spring提供的事务传播策略中的新建事务。只有全局事务的发起者才能开启一个全局事务，并根据是否产生回滚异常来决定是否全局事务的提交或全局事务的回滚</strong></p><h3 id="2-5-全局事务的参与者"><a href="#2-5-全局事务的参与者" class="headerlink" title="2.5 全局事务的参与者"></a>2.5 全局事务的参与者</h3><p>​<strong>处于微服务调用链中全局事务内部，参与到了其他上游服务发起的全局事务（这时线程上下文存在xid）。类比spring提供的事务传播策略中的加入了已存在的事务，所以，全局事务的参与者不能触发全局事务的回滚或提交。只能由RM来报告当前分支事务的状态（触发rollback后，分支事务状态变为PhaseOne_Failed）</strong></p><h3 id="2-6-分支事务和全局事务"><a href="#2-6-分支事务和全局事务" class="headerlink" title="2.6 分支事务和全局事务"></a>2.6 分支事务和全局事务</h3><p>全局事务：整个分布式事务，由多个分支事务组成</p><p>分支事务：分布式事务中整个链路里每个服务单独的事务，不可能跨服务存在。运行在spring事务的支持下，一个本地事务就是一个分支事务。没有被本地事务支持但运行在全局事务下，则一个dml sql就会生成一个分支事务</p><h2 id="三、服务端初始化"><a href="#三、服务端初始化" class="headerlink" title="三、服务端初始化"></a>三、服务端初始化</h2><p>服务端就是一个springboot项目，启动类是<strong>ServerApplication，核心启动方法是io.seata.server.Server#start</strong></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// io.seata.server.Server#start 方法里的一些主要功能（删除了些不重要的）</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">start</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 统计相关功能初始化</span></span><br><span class="line">    MetricsManager.get().init();</span><br><span class="line">    <span class="comment">// 设置数据保存模式（db，file，或redis）</span></span><br><span class="line">    System.setProperty(ConfigurationKeys.STORE_MODE, parameterParser.getStoreMode());</span><br><span class="line"></span><br><span class="line">    <span class="type">ThreadPoolExecutor</span> <span class="variable">workingThreads</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ThreadPoolExecutor</span>(NettyServerConfig.getMinServerPoolSize(),</span><br><span class="line">            NettyServerConfig.getMaxServerPoolSize(), NettyServerConfig.getKeepAliveTime(), TimeUnit.SECONDS,</span><br><span class="line">            <span class="keyword">new</span> <span class="title class_">LinkedBlockingQueue</span>&lt;&gt;(NettyServerConfig.getMaxTaskQueueSize()),</span><br><span class="line">            <span class="keyword">new</span> <span class="title class_">NamedThreadFactory</span>(<span class="string">&quot;ServerHandlerThread&quot;</span>, NettyServerConfig.getMaxServerPoolSize()), <span class="keyword">new</span> <span class="title class_">ThreadPoolExecutor</span>.CallerRunsPolicy());</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 创建netty相关对象</span></span><br><span class="line">    <span class="type">NettyRemotingServer</span> <span class="variable">nettyRemotingServer</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">NettyRemotingServer</span>(workingThreads);</span><br><span class="line">    nettyRemotingServer.setListenPort(parameterParser.getPort());</span><br><span class="line">    <span class="comment">// 创建TC</span></span><br><span class="line">    <span class="type">DefaultCoordinator</span> <span class="variable">coordinator</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">DefaultCoordinator</span>(nettyRemotingServer);</span><br><span class="line">    <span class="comment">// 启动一些定时任务，包括 异步全局事务commit操作等</span></span><br><span class="line">    coordinator.init();</span><br><span class="line">    nettyRemotingServer.setHandler(coordinator);</span><br><span class="line">    <span class="comment">//127.0.0.1 and 0.0.0.0 are not valid here.</span></span><br><span class="line">    <span class="comment">// 设置服务端netty的ip和端口</span></span><br><span class="line">    <span class="keyword">if</span> (NetUtil.isValidIp(parameterParser.getHost(), <span class="literal">false</span>)) &#123;</span><br><span class="line">        XID.setIpAddress(parameterParser.getHost());</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        XID.setIpAddress(NetUtil.getLocalIp());</span><br><span class="line">    &#125;</span><br><span class="line">    XID.setPort(nettyRemotingServer.getListenPort());</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 注册消息处理器并构建netty的ServerBootstrap，开启端口的监听</span></span><br><span class="line">    nettyRemotingServer.init();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="四、客户端初始化"><a href="#四、客户端初始化" class="headerlink" title="四、客户端初始化"></a>四、客户端初始化</h2><p>​客户端初始化主要是通过两个自动代理bean来实现的</p><ul><li><strong>GlobalTransactionScanner</strong>：代理**@GlobalTransactional和@GlobalLock注解标志的类或方法，统一使用GlobalTransactionalInterceptor进行拦截**</li><li><strong>SeataAutoDataSourceProxyCreator</strong>：代理DataSource类。<strong>原来执行sql的流程 DataSource -&gt; Connection -&gt; Statement -&gt; 开始执行sql，变为了DataSourceProxy -&gt; ConnectionProxy -&gt; StatementProxy</strong>（<strong>DataSourceProxy里代理commit相关操作，和server通信。StatementProxy则代理执行sql的逻辑，处理执行前后镜像数据并生成undo数据，以便后续的rollback操作</strong>）</li></ul><h2 id="五、一个完整的全局事务流程（db模式）"><a href="#五、一个完整的全局事务流程（db模式）" class="headerlink" title="五、一个完整的全局事务流程（db模式）"></a>五、一个完整的全局事务流程（db模式）</h2><h3 id="5-1-GlobalTransactional注解拦截"><a href="#5-1-GlobalTransactional注解拦截" class="headerlink" title="5.1 @GlobalTransactional注解拦截"></a>5.1 @GlobalTransactional注解拦截</h3><p>拦截器：GlobalTransactionalInterceptor</p><blockquote><p>​这里有个参数：<strong>client.tm.degradeCheck</strong>（是否开启降级检查，默认为false）。</p><p>​当启用该参数时，会开启一个定时任务，间隔时间受参数<strong>client.tm.degradeCheckPeriod</strong>控制（默认为2000毫秒），向服务端发送全局事务的开启和提交测试，以此来判断服务端的全局事务当前是否支持。</p><p>​如果出现<strong>client.tm.degradeCheckAllowTimes</strong>次连续检查失败，则禁用当前全局事务支持，不走GlobalTransactionalInterceptor的代理逻辑。</p><p>​而当降级后，又需要连续检查成功<strong>client.tm.degradeCheckAllowTimes</strong>次才会再次开启全局事务的支持</p></blockquote><ol><li><p>通过上面的参数检查，判断当前请求是否支持全局事务，当支持全局事务时，优先走@GlobalTransactional逻辑，因为全局事务也包括了数据库锁，其次再走@GlobalLock逻辑。</p></li><li><p>解析@GlobalTransactional注解，获取当前环境的xid（如果存在xid则代表当前全局事务是参与者，发起者为上游服务，创建DefaultGlobalTransaction为参与者角色。不存在则代表当前全局事务是发起者）</p></li><li><p>传播策略的检查和支持，类似spring，锁重试设置，开启全局事务</p></li></ol><h3 id="5-2-全局事务开启"><a href="#5-2-全局事务开启" class="headerlink" title="5.2 全局事务开启"></a>5.2 全局事务开启</h3><p><strong>io.seata.tm.api.GlobalTransaction#begin</strong> 方法</p><ol><li>判断当前全局事务的角色，全局事务的发起者才需要真正的开启全局事务，而全局事务的参与者不需要开启全局事务，只需要等待分支事务提交时加入到当前xid的全局事务就行</li><li>全局事务的发起者此时还未向服务端发起的，所以xid肯定为空，先校验一下</li><li>向服务端发送全局事务开启的请求</li><li>服务端收到这个全局事务的开启请求后，创建GlobalSession（根据ID生成器生成唯一的transactionId和xid），并将GlobalSession转换为GlobalTransactionDO在保存到数据库的global_table表中，至此，全局事务开启，信息保存成功，最后再将xid返回给客户端</li><li>客户端收到服务端传来的xid，将其保存到当前线程的上下文中。并应该在后续的调用其他业务服务时传递该xid，串联起来这个全局事务</li></ol><h3 id="5-3-执行业务代码"><a href="#5-3-执行业务代码" class="headerlink" title="5.3 执行业务代码"></a>5.3 执行业务代码</h3><p>​任何一个SQL的执行操作，无非就是先获取DataSource，在通过DataSource获取Connection，通过Connection获取Statement，Statement准备sql和参数，再执行Statement并处理结果集，返回结果</p><p>​所以，追根溯源，要支持分布式事务，就需要控制DataSource的一系列的行为，<strong>SeataAutoDataSourceProxyCreator</strong>就在这里发挥了作用</p><h4 id="1、数据源的自动代理，SeataAutoDataSourceProxyCreator类，wrapIfNecessary方法"><a href="#1、数据源的自动代理，SeataAutoDataSourceProxyCreator类，wrapIfNecessary方法" class="headerlink" title="1、数据源的自动代理，SeataAutoDataSourceProxyCreator类，wrapIfNecessary方法"></a>1、数据源的自动代理，SeataAutoDataSourceProxyCreator类，wrapIfNecessary方法</h4><p>逻辑：只对DataSource代理，且会进行重复代理的判断，构造切面：SeataAutoDataSourceProxyAdvice。最终将原DataSource和SeataDataSourceProxy的关系放入DataSourceProxyHolder类中，方便后续的取出</p><h4 id="2、数据源方法的切入"><a href="#2、数据源方法的切入" class="headerlink" title="2、数据源方法的切入"></a>2、数据源方法的切入</h4><p><strong>SeataAutoDataSourceProxyAdvice</strong>代理了DataSource类的任何操作，其代理逻辑：</p><pre><code>1. 先判断是否需要走切面逻辑（全局锁需要，且存在AT模式的全局事务），不需要走切面逻辑的直接走原逻辑，通过原DataSource完成一系列操作。2. 需要走代理逻辑的流程：通过当前**DataSource**拿到对应的**SeataDataSourceProxy**（**DataSourceProxyHolder**发挥作用），走对应代理类的相同方法，获取代理的**Connection（ConnectionProxy）**，创建当前Connection的**ConnectionContext**，用来保存当前Connection的全局事务信息。创建**StatementProxy**， 拦截Statement的execute方法，根据druid或其他工具解析sql。最终会走到**io.seata.rm.datasource.exec.AbstractDMLBaseExecutor#doExecute**这个方法执行</code></pre><h4 id="3、Statement的执行"><a href="#3、Statement的执行" class="headerlink" title="3、Statement的执行"></a>3、Statement的执行</h4><ol><li><p>普通的select语句因为不会涉及到数据修改，就不会申请锁资源。所以直接走原Statement直接执行。</p></li><li><p>而其他会修改数据的sql（比如 insert，update，delete，select_for_update等），走代理。具体逻辑：在执行原SQL之前，通过原sql构造出select语句，来查询更新字段的执行前镜像值，封装成TableRecords。之后再执行原SQL，执行后再查询更新字段的执行后镜像值，封装成TableRecords，最后再将两次的TableRecords封装成SQLUndoLog，暂放内存中，等待分支事务commit再写入库</p></li></ol><p><strong>io.seata.rm.datasource.exec.AbstractDMLBaseExecutor#doExecute会对当前Connection是否自动提交做判断，为什么呢？</strong></p><blockquote><p>​假如在一个分布式事务中，我们只使用了seata的@GlobalTransactional注解而没有用spring的@Transactional注解控制本地事务，其实也是支持的。</p><p>​当不存在本地事务时，事务都是走的autoCommit的。seata在这里先判断了一下，如果没开启本地事务，单个语句就为一个事务，seata其实还是设置当前事务为手动提交，设置autoCommit为false（因为还有undo_log的写入），然后走seata流程的分支事务提交。所以，当不存在本地事务，任何一个语句就是一个分支事务。</p><p>​在一个服务中，开启了本地事务的地方算一个分支事务，有相同的xid，而未开启本地事务的代码里，每一个update,delete,insert等语句都算一个分支事务，由seata来控制全局事务的一致性。所以，未开启本地事务但只用seata的全局事务也是可以的，但是性能影响会比较大，因为这里由一个分支事务变成多个分支事务了，每一个分支事务都会涉及到与服务端的通信，数据库锁检查，undo_log的写库等操作。</p></blockquote><h4 id="4、分支事务的提交（rpc通过Netty发送commit到服务端）"><a href="#4、分支事务的提交（rpc通过Netty发送commit到服务端）" class="headerlink" title="4、分支事务的提交（rpc通过Netty发送commit到服务端）"></a>4、分支事务的提交（rpc通过Netty发送commit到服务端）</h4><ol><li><p>锁检查（lock_table表），将变化的数据构造为row_key，一个row_key能唯一确定某一行数据</p></li><li><p>已经被其他全局事务占用了数据锁，则所失败，抛出LockConflictException异常并开始重试，默认重试30次，重试间隔为10ms</p></li><li><p>数据未被其他全局事务上锁，则当前全局事务对数据上锁（写入到lock_table表），创建BranchSession，关联xid，再转换为BranchTransactionDO并保存到branch_table表，在返回给客户端branchId。</p></li><li><p>刷新当前分支事务内存中所有的undo_log到数据库的undo_log表，分支事务commit，清除当前Connection的ConnectionContext里全局事务相关信息。到这里，分支事务就已经commit成功了，且进行了写隔离</p></li></ol><h4 id="5、调用其他业务服务的接口"><a href="#5、调用其他业务服务的接口" class="headerlink" title="5、调用其他业务服务的接口"></a>5、调用其他业务服务的接口</h4><ol><li><p>调用其他服务，需要将xid在服务间传递（当前版本好像还没支持feign的，自己实现也简单，请求头加个TX_XID，把xid传过去就行），其他服务设置了spring的拦截器，获取TX_XID，并绑定到当前线程，等待后续判断</p></li><li><p>其他业务服务的流程和上面一样：解析sql，构造镜像，保存undo_log，锁检查，注册分支事务，commit</p></li></ol><h3 id="5-4-业务代码执行完毕，全局事务的回滚或提交流程"><a href="#5-4-业务代码执行完毕，全局事务的回滚或提交流程" class="headerlink" title="5.4 业务代码执行完毕，全局事务的回滚或提交流程"></a>5.4 业务代码执行完毕，全局事务的回滚或提交流程</h3><h4 id="1、全局事务回滚"><a href="#1、全局事务回滚" class="headerlink" title="1、全局事务回滚"></a>1、全局事务回滚</h4><ol><li><p>业务代码抛出异常，且命中回滚异常，触发全局回滚（io.seata.tm.api.DefaultGlobalTransaction#rollback），但这里<strong>只支持全局事务的发起者调用回滚，全局事务的参与者是不能全局事务回滚的</strong>。所以，只有全局事务的发起者出现了异常并命中回滚策略才会触发全局回滚，全局事务的参与者则不能，所以可能导致参与者回滚了，但不抛异常，发起者没有命中回滚，导致发起者全局事务正常commit了，但部分参与者却回滚了，导致没有全部保证一致性。</p></li><li><p>发送全局事务回滚请求到服务端，回滚逻辑</p></li></ol><blockquote><ol><li>GlobalSession的状态校验</li><li>获取这个GlobalSession的所有BranchSession，遍历触发每一个分支事务的回滚</li><li>分支事务回滚，发送请求给客户端，找到当前分支事务的undo_log，对镜像前后进行比较，不同的需要补偿（update就用update，insert用delete，delete用insert等），还原事务前的数据</li><li>分支事务的undo_log删除，返回状态给服务端</li><li>服务端对状态的判断和处理，删除分支事务（branch_session）</li></ol></blockquote><h4 id="2、全局事务提交"><a href="#2、全局事务提交" class="headerlink" title="2、全局事务提交"></a>2、全局事务提交</h4><ol><li><p>类似全局事务回滚，也只有全局事务的发起者才能真正的提交</p></li><li><p>发送全局事务提交请求到服务端，提交逻辑</p></li></ol><blockquote><ol><li>释放相关的锁资源：遍历当前全局事务的所有分支事务，逐个将每个分支事务关联的锁资源删除（lock_table表相关的记录）</li><li>判断是否支持异步提交（AT模式是支持的，因为现在锁资源已经释放了，不用在担心会影响到其他全局事务，剩下的只需要把相关的undo_log删除即可），将globalSession状态更新为异步提交中，并更新到数据库中（global_table表）去，在直接返回给客户端已提交</li><li>异步提交：由定时任务线程池触发（Server启动时创建的DefaultCoordinator初始化异步提交定时任务下线程池，默认定时任务时间间隔为1秒），查询所有的异步提交状态的global_session，并将其关联的branch_session全部查出来，遍历每个global_session并开始全局事务的提交</li><li>全局事务的真正提交：遍历global_session中关联的每一个branch_session，发送提交任务到对应的客户端（根据resourceId和clientId查找对应客户端的Channel），客户端收到全局事务的commit请求，将请求封装成Phase2Context对象并扔进待提交队列，等待客户端的定时任务线程池提交，然后直接返回给服务端分支事务已提交的状态</li><li>客户端定时任务线程池提交全局事务逻辑：将所有的Phase2Context按资源分组（在seata的AT模式下，一个资源就可以代表一个数据库），具有相同资源的待提交任务处于同一个数据库里，可以用相同的Connection。通过资源管理器获取获取数据源代理，再通过这个数据源代理拿到原Connection（以免这些更新语句又走了代理，创建了undo_log），在批量删除对应的undo_log。</li><li>当所有分支事务提交完成后（undo_log删除完成），最后再server端删除对应的GlobalSession记录</li></ol></blockquote><h3 id="5-5-全局事务结束"><a href="#5-5-全局事务结束" class="headerlink" title="5.5 全局事务结束"></a>5.5 全局事务结束</h3><ol><li><p>如果当前全局事务是嵌套的，需要恢复上一个全局事务的状态（包括全局锁配置：锁失败的重试次数和时间间隔，恢复上一个全局事务的xid）</p></li><li><p>触发回调函数，和清除当前全局事务的钩子函数</p></li></ol><p>至此，全局事务的流程已全部结束</p><h2 id="六、全局事务锁冲突和重试的实现"><a href="#六、全局事务锁冲突和重试的实现" class="headerlink" title="六、全局事务锁冲突和重试的实现"></a>六、全局事务锁冲突和重试的实现</h2><h3 id="6-1-写隔离和锁重试的实现"><a href="#6-1-写隔离和锁重试的实现" class="headerlink" title="6.1 写隔离和锁重试的实现"></a>6.1 写隔离和锁重试的实现</h3><p>​seata在客户端执行更新数据相关的sql时，会记录sql操作前后的镜像值到undo_log中，同时据此准备lockKeys暂存起来，lockKeys就是变化的数据唯一标识（比如表明，主键列的值等）。在客户端的本地事务提交时，先注册当前分支事务（<strong>利用RM的branchRegister接口</strong>）。seata服务端的TC开始尝试注册分支事务，尝试步骤：</p><ul><li><p>先对全局事务进行状态检查</p></li><li><p>利用客户端传来的信息构造BranchSession</p></li><li><p><strong>对BranchSession尝试加锁（AT模式才会）</strong></p></li><li><p><strong>通过对锁信息的一系列检查和转化，到达LockStoreDataBaseDAO#acquireLock(java.util.List&lt;io.seata.core.store.LockDO&gt;)接口</strong></p></li><li><p>查询lock_table中对应row_key存在的数据（这个row_key就是行锁，能唯一标识客户端某个数据库里某个表的某行数据）</p><ul><li><p>如果没查询出来，代表写锁不冲突，将这些锁资源保存在lock_table中，直接返回加锁成功</p></li><li><p>如果查询出来了，且这些数据还是当前全局事务（xid一致），代表重复枷锁而已，只需要将原本不存在的所数据保存到lock_table中，就可返回枷锁成功</p></li><li><p>查询出来，且有其他全局事务占用当前资源，直接返回加锁失败。由io.seata.server.transaction.at.ATCore#branchSessionLock直接抛出异常码为LockKeyConflict的异常，代表写锁冲突</p></li></ul></li><li><p>加锁成功就不说了，直接向下走。<strong>而加锁失败，由服务端的AbstractCallback捕获异常，并转化为对应消息，发送给客户端</strong></p></li><li><p><strong>客户端的ConnectionProxy#recognizeLockKeyConflictException方法检测到是锁冲突异常，又抛出LockConflictException异常</strong></p></li><li><p><strong>客户端的LockRetryPolicy#doRetryOnLockConflict捕获到LockConflictException异常，开始重试（默认重试30次，间隔时间为10毫秒）</strong></p></li><li><p><strong>如果30次还是重试失败，转为LockWaitTimeoutException异常，触发客户端本地事务的回滚</strong></p></li></ul><h2 id="7-思考"><a href="#7-思考" class="headerlink" title="7 思考"></a>7 思考</h2><h3 id="7-1-必须保证全局事务范围大于等于分支事务（spring提供的事务注解等）"><a href="#7-1-必须保证全局事务范围大于等于分支事务（spring提供的事务注解等）" class="headerlink" title="7.1 必须保证全局事务范围大于等于分支事务（spring提供的事务注解等）"></a>7.1 必须保证全局事务范围大于等于分支事务（spring提供的事务注解等）</h3><p>​假如全局事务的范围小于分支事务：全局事务的发起者先是进行全局事务的提交，再进行本地事务的提交。在全局事务提交时，本地事务都还没提交（本地事务提交时才会注册当前分支事务到server端），所以在server端不会有任何当前全局事务发起者客户端的分支事务，在server端触发分支事务提交时也不会发送给当前客户端，所以，对于当前客户端来说全局事务的提交就是个空提交（但对其他全局事务的参与者不是）。而全局事务在server端的提交又是个异步的过程，所以当客户端提交本地事务时，可能会出现以下错误</p><ul><li>全局事务提交完毕**：server的全局事务操作完毕后会删掉GlobalSessiob，所以，当客户端的本地事务提交，向server端注册分支事务时，会导致xid不存在异常（这个xid已经随着全局事务的终结给删掉了）**</li><li>全局事务正在提交：<strong>虽然在server端全局事务是异步提交的，但在这之前全局事务的状态会先设为AsyncCommitting（这一步和客户端提交全局事务是同步的）。所以，在客户端向server注册分支事务时，server端会先校验全局事务xid的状态，发现全局事务状态不为Begin，抛出全局事务异常</strong></li></ul><p>​为什么范围可以等于，因为全局事务拦截器的<strong>io.seata.spring.annotation.GlobalTransactionalInterceptor#getPosition接口默认将@GlobalTransactional拦截放在了@Transactional的前面，拦截器也类似栈结构（先进后出）</strong>。先进入@GlobalTransactional拦截方法，再进入@Transactional拦截方法，先从@Transactional拦截方法出，之后再从@GlobalTransactional方法出来。保证了在全局事务提交前，本地事务已经注册了。</p><p><strong>综上，@GlobalTransactional和spring的@Transactional的正确搭配：</strong></p><p>​@GlobalTransactional范围要大于等于@Transactional，最好是两个作用域一样（即可保证事务正常进行，又可减少客户端的分支事务，减少与server端的通信等）</p><h3 id="7-2-全局事务的参与者回滚异常一定让全局事务的发起者感知到"><a href="#7-2-全局事务的参与者回滚异常一定让全局事务的发起者感知到" class="headerlink" title="7.2 全局事务的参与者回滚异常一定让全局事务的发起者感知到"></a>7.2 全局事务的参与者回滚异常一定让全局事务的发起者感知到</h3><p>​由于全局事务的参与者本地rollback时，只会通过RM向服务端报告当前分支是的状态为PhaseOne_Failed。如果参与者本地回滚后，全局事务的发起者不能感知到参与者的回滚，而触发了全局事务发起者的commit，这时在服务端的io.seata.server.coordinator.DefaultCore#doGlobalCommit方法中（全局事务发起者向server端发送全局事务提交），TC获取到全局事务的所有分支事务时，如果某个分支事务状态为PhaseOne_Failed（即这个分支事务本地rollback了），TC也仅仅是删除这个分支事务，不会触发全局回滚，全局事务从整体上来说还是提交，就造成了参发起者commit，这个参与者rollback，分布式事务逻辑上就失败了。</p><p>​所以，参与者的回滚一定要让发起者感知到，并让发起者触发全局事务的回滚，这样，全局事务才是原子性的操作。</p><h3 id="7-3-seata做的一系列程序健壮性支持"><a href="#7-3-seata做的一系列程序健壮性支持" class="headerlink" title="7.3 seata做的一系列程序健壮性支持"></a>7.3 seata做的一系列程序健壮性支持</h3><h4 id="7-3-1-降级检查"><a href="#7-3-1-降级检查" class="headerlink" title="7.3.1 降级检查"></a>7.3.1 降级检查</h4><p>​降级检查是否开启由配置参数client.tm.degradeCheck管理，应用到GlobalTransactionalInterceptor#degradeCheck字段上，且支持热修改（比如consul上修改了这个参数，可以同步到客户端，实现不重启就可以开启或关闭降级检查），默认为false，表示不开启降级检查</p><p>​当开启降级检查时，在GlobalTransactionalInterceptor的构造器里会读取配置参数degradeCheckPeriod和degradeCheckAllowTimes，分别表示降级检查一次的时间间隔和连续超过指定次数后，将关闭或开启全局事务代理模式。再提交一个定时任务，去执行降级检查（代码实现GlobalTransactionalInterceptor#startDegradeCheck里）</p><p>GlobalTransactionalInterceptor#startDegradeCheck逻辑：</p><p>​按指定的degradeCheckPeriod毫秒时间间隔执行这个任务，里面开启一个空的全局事务，并马上提交（测试全局事务是否正常），根据是否通过来利用事件发布和通知机制（DegradeCheckEvent事件），通知对应得事件监听器处理对应得逻辑。监听代码在io.seata.spring.annotation.GlobalTransactionalInterceptor#onDegradeCheck里实现，连续失败degradeCheckAllowTimes次会开启降级，而在降级期间内，若连续成功degradeCheckAllowTimes则会取消降级。降级具体应用在io.seata.spring.annotation.GlobalTransactionalInterceptor#invoke，如果降级开启，则会禁用全局事务</p><h4 id="7-3-2-写锁冲突重试机制"><a href="#7-3-2-写锁冲突重试机制" class="headerlink" title="7.3.2 写锁冲突重试机制"></a>7.3.2 写锁冲突重试机制</h4><p>​参考6.1</p><h4 id="7-3-3-全局事务commit和rollback失败重试机制"><a href="#7-3-3-全局事务commit和rollback失败重试机制" class="headerlink" title="7.3.3 全局事务commit和rollback失败重试机制"></a>7.3.3 全局事务commit和rollback失败重试机制</h4><p>​全局事务提交和回滚的重试机制在io.seata.tm.api.DefaultGlobalTransaction里的commot和rollback里实现。受client.tm.commitRetryCount配置参数控制，默认重试5次。</p><h3 id="7-4-seata的分布式事务隔离级别"><a href="#7-4-seata的分布式事务隔离级别" class="headerlink" title="7.4 seata的分布式事务隔离级别"></a>7.4 seata的分布式事务隔离级别</h3><p>​传统数据库如oracle默认读已提交，mysql则通过mvcc和undo log实现了可重复读隔离级别，而<strong>seata实现的分布式事务也可工作在读已提交的隔离级别下</strong></p><p>如何实现的：</p><blockquote><p>​seata的分支事务进行本地commit操作时，会将改动的的数据构造成lockKeys，在分支事务注册到server时带上这些lockKeys，server端会对这些lockKey进行解析构建，并对其进行上锁，上锁成功的标志是成功将全部lockKeys保存到server短的lock_table表（可上锁的前提是lock_table表没有对应的lockKey数据）。在之后的分支事务的数据修改都会先对这些数据在server端上锁判断，以此实现类似数据库的互斥锁。</p><p>​但是，当分支事务A本地commit后，但此时全局事务还未commit。如果有另一个事务B读取到了事务A修改的数据结果，就会产生脏读（因为这时全局事务还未commit，甚至可能触发rollback操作），解决脏读的办法就是事务B读取使用select for update语句，并且被@GlobalTransactional和@GlobalLock注解拦截。拦截后seata内部会对select for update的操作进行锁判断，生成一个SelectForUpdateExecutor执行器（核心执行方法doExecute代码如下）。所以直到分支事务A对应的全局事务结束释放了锁资源后，事务B才会读到数据并返回，以此实现读已提交</p></blockquote><p>注意**，@GlobalTransactional + select for update<strong>和</strong>@GlobalLock + select for update都可实现select操作的读已提交隔离级别**，<strong>但GlobalLock 更轻量级，它不会注册分支事务和加锁等操作，只会进行锁检查。所以，如果上述事务B不需要运行在全局事务或本地事务的模式先下而又想实现全局事务的读已提交隔离级别，就可使用@GlobalLock + select for update组合</strong></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// SelectForUpdateExecutor核心执行方法</span></span><br><span class="line"><span class="keyword">public</span> T <span class="title function_">doExecute</span><span class="params">(Object... args)</span> <span class="keyword">throws</span> Throwable &#123;</span><br><span class="line">    <span class="type">Connection</span> <span class="variable">conn</span> <span class="operator">=</span> statementProxy.getConnection();</span><br><span class="line">    <span class="type">DatabaseMetaData</span> <span class="variable">dbmd</span> <span class="operator">=</span> conn.getMetaData();</span><br><span class="line">    T rs;</span><br><span class="line">    <span class="type">Savepoint</span> <span class="variable">sp</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line">    <span class="type">boolean</span> <span class="variable">originalAutoCommit</span> <span class="operator">=</span> conn.getAutoCommit();</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        <span class="comment">// 在这里单独开启事务。如果没有原事务则直接开启，如果有则新建一个savepoint来开启事务</span></span><br><span class="line">        <span class="keyword">if</span> (originalAutoCommit) &#123; <span class="comment">// 没有原事务</span></span><br><span class="line">            conn.setAutoCommit(<span class="literal">false</span>);</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (dbmd.supportsSavepoints()) &#123; <span class="comment">// 有原事务，新建一个savepoint来开启内嵌事务</span></span><br><span class="line">            sp = conn.setSavepoint();</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">SQLException</span>(<span class="string">&quot;not support savepoint. please check your db version&quot;</span>);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="type">LockRetryController</span> <span class="variable">lockRetryController</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">LockRetryController</span>();</span><br><span class="line">        ArrayList&lt;List&lt;Object&gt;&gt; paramAppenderList = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">        <span class="type">String</span> <span class="variable">selectPKSQL</span> <span class="operator">=</span> buildSelectSQL(paramAppenderList);</span><br><span class="line">        <span class="keyword">while</span> (<span class="literal">true</span>) &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                <span class="comment">// #870</span></span><br><span class="line">                <span class="comment">// execute return</span></span><br><span class="line">                <span class="comment">// executeQuery return ResultSet</span></span><br><span class="line">                <span class="comment">// 执行sql语句，会获取本地锁</span></span><br><span class="line">                rs = statementCallback.execute(statementProxy.getTargetStatement(), args);</span><br><span class="line"></span><br><span class="line">                <span class="comment">// Try to get global lock of those rows selected</span></span><br><span class="line">                <span class="type">TableRecords</span> <span class="variable">selectPKRows</span> <span class="operator">=</span> buildTableRecords(getTableMeta(), selectPKSQL, paramAppenderList);</span><br><span class="line">                <span class="comment">// 构建lock key</span></span><br><span class="line">                <span class="type">String</span> <span class="variable">lockKeys</span> <span class="operator">=</span> buildLockKey(selectPKRows);</span><br><span class="line">                <span class="keyword">if</span> (StringUtils.isNullOrEmpty(lockKeys)) &#123; <span class="comment">// lock key不存在，代表没有锁冲突，直接就break了</span></span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                &#125;</span><br><span class="line"></span><br><span class="line">                <span class="keyword">if</span> (RootContext.inGlobalTransaction() || RootContext.requireGlobalLock()) &#123; </span><br><span class="line">                    <span class="comment">// 全局锁的获取和判断必须在@GlobalTransactional or @GlobalLock注解下</span></span><br><span class="line">                    <span class="comment">// 只进行锁检查，不加锁（检查失败内部会抛异常）</span></span><br><span class="line">                    statementProxy.getConnectionProxy().checkLock(lockKeys);</span><br><span class="line">                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">RuntimeException</span>(<span class="string">&quot;Unknown situation!&quot;</span>);</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="comment">// 走到这就代表全局锁没有冲突（要查询的数据没有被多个事务同时处理），可直接返回了</span></span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            &#125; <span class="keyword">catch</span> (LockConflictException lce) &#123;</span><br><span class="line">                <span class="comment">// 全局锁冲突，本地事务先rollback，释放select .. for update获取到的锁</span></span><br><span class="line">                <span class="keyword">if</span> (sp != <span class="literal">null</span>) &#123;</span><br><span class="line">                    conn.rollback(sp);</span><br><span class="line">                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    conn.rollback();</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="comment">// sleep后会再进行重试</span></span><br><span class="line">                <span class="comment">// trigger retry</span></span><br><span class="line">                lockRetryController.sleep(lce);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        <span class="comment">// 复原现场</span></span><br><span class="line">        <span class="keyword">if</span> (sp != <span class="literal">null</span>) &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                <span class="keyword">if</span> (!JdbcConstants.ORACLE.equalsIgnoreCase(getDbType())) &#123;</span><br><span class="line">                    conn.releaseSavepoint(sp);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125; <span class="keyword">catch</span> (SQLException e) &#123;</span><br><span class="line">                LOGGER.error(<span class="string">&quot;&#123;&#125; release save point error.&quot;</span>, getDbType(), e);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (originalAutoCommit) &#123;</span><br><span class="line">            conn.setAutoCommit(<span class="literal">true</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> rs;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>demo</title>
      <link href="/2025/01/06/demo/"/>
      <url>/2025/01/06/demo/</url>
      
        <content type="html"><![CDATA[]]></content>
      
      
      
    </entry>
    
    
  
  
</search>
